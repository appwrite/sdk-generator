import Foundation
import JSONCodable
{% if definition.properties | filter(p => (p | enumValues) is not empty) | length > 0 %}
import {{spec.info.title | caseUcfirst}}Enums
{% endif %}

/// {{ definition.description }}
{% if definition.properties | length == 0 and not definition.additionalProperties %}
open class {{ definition | modelType(spec) | raw }}: Codable {}
{% else %}
open class {{ definition | modelType(spec) | raw }}: Codable {

    enum CodingKeys: String, CodingKey {
        {%~ for property in definition.properties %}
        case {{ (property | schemaName) | escapeSwiftKeyword | removeDollarSign }} = "{{ (property | schemaName) }}"
        {%~ endfor %}
        {%~ if definition.additionalProperties %}
        case {{ 'data' | escapeSwiftKeyword | removeDollarSign }} = "{{ 'data' }}"
        {%~ endif %}
    }

    {%~ for property in definition.properties %}
    /// {{ property.description }}
    public let {{ (property | schemaName) | escapeSwiftKeyword | removeDollarSign }}: {{ property | propertyType(spec) | raw }}{% if not (property | schemaRequired) %}?{% endif %}

    {%~ endfor %}
    {%~ if definition.additionalProperties %}
    /// Additional properties
    public let {{ 'data' | escapeSwiftKeyword | removeDollarSign }}: T
    {%~ endif %}

    init(
        {%~ for property in definition.properties %}
        {{ (property | schemaName) | escapeSwiftKeyword | removeDollarSign }}: {{ property | propertyType(spec) | raw  }}{% if not (property | schemaRequired) %}?{% endif %}{% if not loop.last or (loop.last and definition.additionalProperties) %},{% endif %}

        {%~ endfor %}
        {%~ if definition.additionalProperties %}
        {{ 'data' | escapeSwiftKeyword | removeDollarSign }}: T
        {%~ endif %}
    ) {
        {%~ for property in definition.properties %}
        self.{{ (property | schemaName) | escapeSwiftKeyword | removeDollarSign }} = {{ (property | schemaName) | escapeSwiftKeyword | removeDollarSign }}
        {%~ endfor %}
        {%~ if definition.additionalProperties %}
        self.{{ 'data' | escapeSwiftKeyword | removeDollarSign }} = {{ 'data' | escapeSwiftKeyword | removeDollarSign }}
        {%~ endif %}
    }

    public required init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)

        {%~ for property in definition.properties %}
        {%~ if (property | schemaType) != 'array' and (property | enumValues) is not empty %}
        {%~ if (property | schemaRequired) %}
        self.{{ (property | schemaName) | escapeSwiftKeyword | removeDollarSign }} = {{ property | propertyType(spec) | raw }}(rawValue: try container.decode(String.self, forKey: .{{ (property | schemaName) | escapeSwiftKeyword | removeDollarSign }}))!
        {%~ else %}
        if let {{ (property | schemaName) | escapeSwiftKeyword | removeDollarSign }}String = try container.decodeIfPresent(String.self, forKey: .{{ (property | schemaName) | escapeSwiftKeyword | removeDollarSign }}) {
            self.{{ (property | schemaName) | escapeSwiftKeyword | removeDollarSign }} = {{ property | propertyType(spec) | raw }}(rawValue: {{ (property | schemaName) | escapeSwiftKeyword | removeDollarSign }}String)
        } else {
            self.{{ (property | schemaName) | escapeSwiftKeyword | removeDollarSign }} = nil
        }
        {%~ endif %}
        {%~ elseif (property | enumValues) %}
        {%~ set enumName = (property | enumName) %}
        {%~ if (property | schemaRequired) %}
        self.{{ (property | schemaName) | escapeSwiftKeyword | removeDollarSign }} = try container.decode([String].self, forKey: .{{ (property | schemaName) | escapeSwiftKeyword | removeDollarSign }}).map { {{ spec.info.title | caseUcfirst }}Enums.{{ enumName | caseUcfirst }}(rawValue: $0)! }
        {%~ else %}
        if let {{ (property | schemaName) | escapeSwiftKeyword | removeDollarSign }}Strings = try container.decodeIfPresent([String].self, forKey: .{{ (property | schemaName) | escapeSwiftKeyword | removeDollarSign }}) {
            self.{{ (property | schemaName) | escapeSwiftKeyword | removeDollarSign }} = {{ (property | schemaName) | escapeSwiftKeyword | removeDollarSign }}Strings.map { {{ spec.info.title | caseUcfirst }}Enums.{{ enumName | caseUcfirst }}(rawValue: $0)! }
        } else {
            self.{{ (property | schemaName) | escapeSwiftKeyword | removeDollarSign }} = nil
        }
        {%~ endif %}
        {%~ else %}
        self.{{ (property | schemaName) | escapeSwiftKeyword | removeDollarSign }} = try container.decode{% if not (property | schemaRequired) %}IfPresent{% endif %}({{ property | propertyType(spec) | raw }}.self, forKey: .{{ (property | schemaName) | escapeSwiftKeyword | removeDollarSign }})
        {%~ endif %}
        {%~ endfor %}
        {%~ if definition.additionalProperties %}
        self.{{ 'data' | escapeSwiftKeyword | removeDollarSign }} = try container.decode(T.self, forKey: .{{ 'data' | escapeSwiftKeyword | removeDollarSign }})
        {%~ endif %}
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)

        {%~ for property in definition.properties %}
        try container.encode{% if not (property | schemaRequired) %}IfPresent{% endif %}({{ (property | schemaName) | escapeSwiftKeyword | removeDollarSign }}{% if (property | schemaType) != 'array' and (property | enumValues) is not empty %}{% if (property | schemaRequired) %}.rawValue{% else %}?.rawValue{% endif %}{% elseif (property | enumValues) is not empty %}{% if (property | schemaRequired) %}.map { $0.rawValue }{% else %}?.map { $0.rawValue }{% endif %}{% endif %}, forKey: .{{ (property | schemaName) | escapeSwiftKeyword | removeDollarSign }})
        {%~ endfor %}
        {%~ if definition.additionalProperties %}
        try container.encode({{ 'data' | escapeSwiftKeyword | removeDollarSign }}, forKey: .{{ 'data' | escapeSwiftKeyword | removeDollarSign }})
        {%~ endif %}
    }

    public func toMap() -> [String: Any] {
        return [
            {%~ for property in definition.properties %}
            "{{ (property | schemaName) }}": {{ property | modelToMapValue(property | schemaName, property | schemaRequired) }} as Any{% if not loop.last or (loop.last and definition.additionalProperties) %},{% endif %}

            {%~ endfor %}
            {%~ if definition.additionalProperties %}
            "{{ 'data' }}": (try! JSONSerialization.jsonObject(with: JSONEncoder().encode({{ 'data' | escapeSwiftKeyword | removeDollarSign }}))) as? [String: Any] ?? [:]
            {%~ endif %}
        ]
    }

    public static func from(map: [String: Any] ) -> {{ definitionName | caseUcfirst }} {
        return {{ definitionName | caseUcfirst }}(
            {%~ for property in definition.properties %}
            {%~ set isDocument = definitionName | lower == 'document' %}{# Temporary Fix until BE is fixed to return all attributes #}
            {{ (property | schemaName) | removeDollarSign }}: {% if (property | schemaModel) -%}
                {%- if (property | schemaType) == 'array' -%}
                    (map["{{(property | schemaName) }}"] as{% if isDocument %}?{% else %}{% if (property | schemaRequired) %}!{% else %}?{% endif %}{% endif %} [[String: Any]]{% if isDocument %} ?? []{% elseif not (property | schemaRequired) %} ?? []{% endif %}).map { {{(property | schemaModel) | caseUcfirst}}.from(map: $0) }
                {%- else -%}
                    {% if isDocument or not (property | schemaRequired) %}map["{{(property | schemaName) }}"] as? [String: Any] != nil ? {{(property | schemaModel) | caseUcfirst}}.from(map: map["{{(property | schemaName) }}"] as! [String: Any]) : nil{% else %}{{(property | schemaModel) | caseUcfirst}}.from(map: map["{{(property | schemaName) }}"] as! [String: Any]){% endif %}
                {%- endif -%}
            {%- else -%}
                {%- if property | isAnyCodableArray(spec) -%}
                    (map["{{(property | schemaName) }}"] as{% if isDocument %}?{% else %}{% if (property | schemaRequired) %}!{% else %}?{% endif %}{% endif %} [[String: Any]]{% if isDocument or not (property | schemaRequired) %} ?? []{% endif %}).map { $0.mapValues { AnyCodable($0) } }
                {%- elseif property | isAnyCodableObject(spec) -%}
                    (map["{{(property | schemaName) }}"] as{% if isDocument %}?{% else %}{% if (property | schemaRequired) %}!{% else %}?{% endif %}{% endif %} [String: Any]{% if isDocument or not (property | schemaRequired) %} ?? [:]{% endif %}).mapValues { AnyCodable($0) }
                {%- elseif (property | schemaType) != 'array' and (property | enumValues) is not empty -%}
                    {%- set enumName = (property | enumName) -%}
                    {% if (property | schemaRequired) %}{{ spec.info.title | caseUcfirst }}Enums.{{ enumName | caseUcfirst }}(rawValue: map["{{(property | schemaName) }}"] as{% if isDocument %}?{% else %}!{% endif %} String{% if isDocument %} ?? ""{% endif %})!{% else %}map["{{(property | schemaName) }}"] as? String != nil ? {{ spec.info.title | caseUcfirst }}Enums.{{ enumName | caseUcfirst }}(rawValue: map["{{(property | schemaName) }}"] as! String) : nil{% endif %}
                {%- elseif (property | enumValues) -%}
                    {%- set enumName = (property | enumName) -%}
                    {% if (property | schemaRequired) %}(map["{{(property | schemaName) }}"] as{% if isDocument %}?{% else %}!{% endif %} [String]{% if isDocument %} ?? []{% endif %}).map { {{ spec.info.title | caseUcfirst }}Enums.{{ enumName | caseUcfirst }}(rawValue: $0)! }{% else %}(map["{{(property | schemaName) }}"] as? [String])?.map { {{ spec.info.title | caseUcfirst }}Enums.{{ enumName | caseUcfirst }}(rawValue: $0)! }{% endif %}
                {%- else -%}
                    map["{{(property | schemaName) }}"] as{% if isDocument %}?{% else %}{% if (property | schemaRequired) %}!{% else %}?{% endif %}{% endif %} {{ property | propertyType(spec) | raw }}{% if isDocument and (property | schemaRequired) %}{% if (property | schemaType) == 'string' %} ?? ""{% elseif (property | schemaType) == 'integer' %} ?? 0{% elseif (property | schemaType) == 'number' %} ?? 0.0{% elseif (property | schemaType) == 'boolean' %} ?? false{% elseif (property | schemaType) == 'array' %} ?? []{% endif %}{% endif %}
                {%- endif -%}
            {%- endif %}{% if not loop.last or (loop.last and definition.additionalProperties) %},{% endif %}

            {%~ endfor %}
            {%~ if definition.additionalProperties %}
            {{ 'data' | escapeSwiftKeyword | removeDollarSign }}: try! JSONDecoder().decode(T.self, from: JSONSerialization.data(withJSONObject: map["{{ 'data' }}"] as? [String: Any] ?? map, options: []))
            {%~ endif %}
        )
    }
}
{% endif %}
