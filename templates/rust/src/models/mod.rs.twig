{% for definition in spec.definitions %}
mod {{definition.name | caseSnake}};
{% endfor %} 
mod attribute;

{% for definition in spec.definitions %}
pub use self::{{definition.name | caseSnake}}::{{definition.name|caseUcfirst}};
{% endfor %}
pub use self::attribute::Attribute;