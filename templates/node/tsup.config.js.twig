import { defineConfig } from 'tsup'

module.exports = defineConfig({
  entry: ["src/**/*.ts"],
  sourcemap: true,
  clean: true,
  dts: true,
  bundle: false,
  treeshake: true,
  outDir: "dist",
  outExtension({ format }) {
    return {
      cjs: '.js',
      esm: '.mjs'
    }[format]
  },
  target: "node16",
  format: ['cjs', 'esm']
})