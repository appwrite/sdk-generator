<?php

declare(strict_types=1);

namespace {{ namespace | caseNamespace }}\Services;

use {{ namespace | caseNamespace }}\{{ namespace | split('\\') | last | caseUcfirst}}Exception;
use {{ namespace | caseNamespace }}\Client;
use {{ namespace | caseNamespace }}\Service;
{% set added = [] %}
{% set needsPart = false %}
{% for method in (service | operations) %}
{% if 'multipart/form-data' in (method | consumes) %}
{% set needsPart = true %}
{% endif %}
{% for parameter in (method | parameters('all')) %}
{% if (parameter | schemaType) == 'file' and 'InputFile' not in added %}
use {{ namespace | caseNamespace }}\InputFile;
{% set added = added|merge(['InputFile']) %}
{% endif %}
{% if (parameter | enumValues) is not empty and (parameter | schemaType) != 'array' %}
{% if (parameter | enumName) not in added %}
use {{ namespace | caseNamespace }}\Enums\{{ (parameter | enumName) | caseUcfirst }};
{% set added = added|merge([(parameter | enumName)]) %}
{% endif %}
{% endif %}
{% if (parameter | schemaModel) is not empty and (parameter | schemaType) != 'array' %}
{% if (parameter | schemaModel) not in added %}
use {{ namespace | caseNamespace }}\Models\{{ (parameter | schemaModel) | caseUcfirst | overrideIdentifier }};
{% set added = added|merge([(parameter | schemaModel)]) %}
{% endif %}
{% endif %}
{% endfor %}
{% endfor %}
{% if needsPart %}
use Utopia\Psr7\Request\Multipart\Part;
{% endif %}

class {{ service.name | caseUcfirst }} extends Service
{
{% set nonDeprecatedMethodNames = [] %}
{% for method in (service | operations) %}
{% if not method.deprecated %}
{% set nonDeprecatedMethodNames = nonDeprecatedMethodNames|merge([(method | methodName) | caseCamel | lower]) %}
{% endif %}
{% endfor %}
{% for method in (service | operations) %}
{% set methodNameLower = (method | methodName) | caseCamel | lower %}
{% if method.deprecated and methodNameLower in nonDeprecatedMethodNames %}
{# Skip deprecated methods that have namespace collisions with non-deprecated methods #}
{% else %}
{% set deprecated_message = '' %}
    /**
{% if method.description %}
{{ method.description | split('\n') | map(line => line | trim) | join('\n') | trim | wrap(75, '     * ') | replace({'     * \n': '     *\n'}) }}
     *
{% endif %}
     * @throws {{ namespace | split('\\') | last | caseUcfirst}}Exception
{% if method.deprecated %}
     *
{%~ if (method | appwrite('deprecated', {})['since'] ?? '') and (method | appwrite('deprecated', {})['replaceWith'] ?? '') %}
     * @deprecated This API has been deprecated since {{ (method | appwrite('deprecated', {})['since'] ?? '') }}. Please use `{{ (method | appwrite('deprecated', {})['replaceWith'] ?? '') | split('.') | last | caseCamel }}` instead.
{%~ else %}
     * @deprecated This API has been deprecated.
{%~ endif %}
{% if (method | appwrite('deprecated', {})['replaceWith'] ?? '') %}
     * @see {{ (method | appwrite('deprecated', {})['replaceWith'] ?? '') | replace({'.': '::'}) | capitalizeFirst }}
{% endif %}
{% endif %}
     */
    public function {{ (method | methodName) | caseCamel }}({{ method | methodParameters }}): {{ method | getReturn(spec) }}
    {
        $apiPath = str_replace(
            [{% for parameter in (method | parameters('path')) %}'{{ '{' }}{{ parameter.name }}{{ '}' }}'{% if not loop.last %}, {% endif %}{% endfor %}],
            [{% for parameter in (method | parameters('path')) %}{% if (parameter.extensions['x-sdk-source']|default('')) == 'security' %}$this->client->getConfig('{{ parameter.extensions['x-sdk-config'] | caseLower }}'){% else %}{% if parameter | usesEnumType %}(string) {% endif %}${{ parameter.name | caseCamel | escapeKeyword }}{% endif %}{% if not loop.last %}, {% endif %}{% endfor %}],
            '{{ method.path }}'
        );
        {%~ for security in (method | securityQueries) %}
        $apiPath .= '{% if loop.first %}?{% else %}&{% endif %}{{ security.name }}=' . urlencode($this->client->getConfig('{{ security.name | caseLower }}'));
        {%~ endfor %}

        {{~ include('php/base/params.twig') -}}
        {%~ if 'multipart/form-data' in (method | consumes) %}
        {{~ include('php/base/requests/file.twig') -}}
        {%~ else %}

        {{~ include('php/base/requests/api.twig') -}}
        {%~ endif %}
    }
        {%~ if not loop.last %}

        {%~ endif %}
        {%~ endif %}
        {%~ endfor %}
}
