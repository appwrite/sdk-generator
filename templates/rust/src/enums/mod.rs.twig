//! Enums for {{ spec.info.title }} SDK

{% for enum in allEnums %}
pub mod {{ enum.title | caseSnake | overrideIdentifier }};
pub use {{ enum.title | caseSnake | overrideIdentifier }}::{{ enum.title | caseUcfirst | overrideIdentifier }};
{% endfor %}
