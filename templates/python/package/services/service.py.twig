from ..service import Service
from urllib.parse import quote
from typing import Any, Dict, List, Optional, Union{% set hasGenericModel = false %}{% for method in (service | operations) %}{% if (method | responseModel) and (method | responseModel) != 'any' and (method | responseModel) | hasGenericType(spec) %}{% set hasGenericModel = true %}{% endif %}{% endfor %}{% if hasGenericModel %}, Type, TypeVar{% endif %}

from ..exception import AppwriteException
from {{ namespace | caseSnake }}.utils.deprecated import deprecated
{% set added = [] %}
{% for method in (service | operations) %}
{% for parameter in (method | parameters('all')) %}
{% if (parameter | schemaType) == 'file' and (parameter | schemaType) not in added %}
from ..input_file import InputFile
{% set added = added|merge(['InputFile']) %}
{% endif %}
{% if (parameter | enumValues) is not empty%}
{% if (parameter | enumName) not in added %}
{% set enumRef = parameter | getServiceEnumName(service, spec) %}
from ..enums.{{ (parameter | enumName) | caseSnake }} import {{ (parameter | enumName) | caseUcfirst }}{% if enumRef != ((parameter | enumName) | caseUcfirst) %} as {{ enumRef }}{% endif %}

{% set added = added|merge([(parameter | enumName)]) %}
{% endif %}
{% endif %}
{% set modelName = (parameter | schemaModel) %}
{% if modelName %}
{% if modelName not in added %}
{% set modelRef = modelName | getServiceModelName(service, spec) %}
from ..models.{{ modelName | caseSnake }} import {{ modelName | caseUcfirst }}{% if modelRef != (modelName | caseUcfirst) %} as {{ modelRef }}{% endif %}

{% set added = added|merge([modelName]) %}
{% endif %}
{% endif %}
{% endfor %}
{% if (method | responseModel) and (method | responseModel) != 'any' %}
{% if (method | responseModel) not in added %}
{% set modelRef = (method | responseModel) | getServiceModelName(service, spec) %}
from ..models.{{ (method | responseModel) | caseSnake }} import {{ (method | responseModel) | caseUcfirst }}{% if modelRef != ((method | responseModel) | caseUcfirst) %} as {{ modelRef }}{% endif %}

{% set added = added|merge([(method | responseModel)]) %}
{% endif %}
{% endif %}
{% if (method | responseModels)|length > 1 %}
{% for responseModel in (method | responseModels) %}
{% if responseModel and responseModel != 'any' and responseModel not in added %}
{% set modelRef = responseModel | getServiceModelName(service, spec) %}
from ..models.{{ responseModel | caseSnake }} import {{ responseModel | caseUcfirst }}{% if modelRef != (responseModel | caseUcfirst) %} as {{ modelRef }}{% endif %}

{% set added = added|merge([responseModel]) %}
{% endif %}
{% endfor %}
{% endif %}
{% endfor %}
{% if hasGenericModel %}

T = TypeVar('T')
{% endif %}


class {{ service.name | caseUcfirst }}(Service):

    def __init__(self, client) -> None:
        super({{ service.name | caseUcfirst }}, self).__init__(client)
{% for method in (service | operations) %}
{% set methodNameSnake = (method | methodName) | caseSnake %}
{# Check if this method should be skipped (is deprecated and has a non-deprecated duplicate) #}
{% set shouldSkip = false %}
{% if method.deprecated %}
{% for otherMethod in (service | operations) %}
{% if not otherMethod.deprecated and ((otherMethod | methodName) | caseSnake) == methodNameSnake %}
{% set shouldSkip = true %}
{% endif %}
{% endfor %}
{% endif %}
{% if not shouldSkip %}
{% set validResponseModels = [] %}
{% if (method | responseModels)|length > 1 %}
{% for responseModel in (method | responseModels) %}
{% if responseModel and responseModel != 'any' %}
{% set validResponseModels = validResponseModels|merge([responseModel]) %}
{% endif %}
{% endfor %}
{% endif %}
{% set isGenericResponse = (method | responseModel) and (method | responseModel) != 'any' and (method | responseModel) | hasGenericType(spec) %}

{% if method.deprecated %}
{% if (method | appwrite('deprecated', {})['since'] ?? '') and (method | appwrite('deprecated', {})['replaceWith'] ?? '') %}
    @deprecated("This API has been deprecated since {{ (method | appwrite('deprecated', {})['since'] ?? '') }}. Please use `{{ (method | appwrite('deprecated', {})['replaceWith'] ?? '') | caseSnakeExceptFirstDot }}` instead.")
{% else %}
    @deprecated("This API has been deprecated.")
{% endif %}
{% endif %}
{% set returnType %}
{% if (method | methodType) == 'webAuth' %}
str
{% elseif (method | methodType) == 'location' %}
bytes
{% elseif validResponseModels|length > 1 %}
Union[{% for responseModel in validResponseModels %}{% if not loop.first %}, {% endif %}{{ responseModel | getServiceModelName(service, spec) }}{% endfor %}]
{% elseif validResponseModels|length == 1 %}
{{ validResponseModels[0] | getServiceModelName(service, spec) }}
{% elseif (method | responseModel) and (method | responseModel) != 'any' %}
{{ (method | responseModel) | getServiceModelName(service, spec) }}{% if isGenericResponse %}[T]{% endif %}
{% else %}
Dict[str, Any]
{% endif %}
{% endset %}
    def {{ (method | methodName) | caseSnake }}(
        self,
{% for parameter in (method | parameters('all')) %}        {{ parameter.name | escapeKeyword | caseSnake }}: {{ parameter | getServicePropertyType(service, spec) | raw }}{% if not parameter.required %} = None{% endif %},
{% endfor %}{% if 'multipart/form-data' in (method | consumes) %}        on_progress=None,
{% endif %}{% if isGenericResponse %}        model_type: Type[T] = dict,
{% endif %}    ) -> {{ returnType | trim | formatModelFieldType | raw }}:
        """
{% if method.description %}        {% autoescape false %}{{ method.description | formatDocstring(8) }}{% endautoescape %}

{% endif %}{% if (method | parameters('all'))|length > 0 or 'multipart/form-data' in (method | consumes) or isGenericResponse %}

{% if method.deprecated %}
{% if (method | appwrite('deprecated', {})['since'] ?? '') and (method | appwrite('deprecated', {})['replaceWith'] ?? '') %}        .. deprecated::{{ (method | appwrite('deprecated', {})['since'] ?? '') }}
            This API has been deprecated since {{ (method | appwrite('deprecated', {})['since'] ?? '') }}. Please use `{{ (method | appwrite('deprecated', {})['replaceWith'] ?? '') | caseSnakeExceptFirstDot }}` instead.
{% else %}        .. deprecated::
            This API has been deprecated.
{% endif %}
{% endif %}        Parameters
        ----------
{% for parameter in (method | parameters('all')) %}        {{ parameter.name | escapeKeyword | caseSnake }} : {{ parameter | getServicePropertyType(service, spec) | raw }}
            {% autoescape false %}{{ parameter.description | formatDocstring(12) }}{% endautoescape %}

{% endfor %}{% if 'multipart/form-data' in (method | consumes) %}        on_progress : callable, optional
            Optional callback for upload progress

{% endif %}{% if isGenericResponse %}        model_type : Type[T], optional
            Pydantic model class for the user-defined data. Defaults to dict for backward compatibility.

{% endif %}{% endif %}        Returns
        -------
{% if (method | methodType) == 'webAuth' %}        str
            Authentication response as a string
{% elseif (method | methodType) == 'location' %}        bytes
            Response as bytes
{% elseif validResponseModels|length > 1 %}        {{ method | getResponseType(service, spec) }}
            API response as one of the typed response models
{% elseif validResponseModels|length == 1 %}        {{ method | getResponseType(service, spec) }}
            API response as a typed Pydantic model
{% elseif (method | responseModel) and (method | responseModel) != 'any' %}{% if isGenericResponse %}        {{ (method | responseModel) | getServiceModelName(service, spec) }}[T]
{% else %}        {{ method | getResponseType(service, spec) }}
{% endif %}            API response as a typed Pydantic model
{% else %}        Dict[str, Any]
            API response as a dictionary
{% endif %}

        Raises
        ------
        AppwriteException
            If API request fails
        """

        api_path = '{{ method.path }}'
        {%~ for security in (method | securityQueries) %}
        api_path += '{% if loop.first %}?{% else %}&{% endif %}{{ security.name }}=' + quote(str(self.client.get_config('{{ security.name | caseLower }}')), safe='')
        {%~ endfor %}
{{ include('python/base/params.twig') }}
{% if 'multipart/form-data' in (method | consumes) %}
{{ include('python/base/requests/file.twig') }}{% else %}{{ include('python/base/requests/api.twig') }}{% endif %}{% endif %}{% if not loop.last %}
{% endif %}{% endfor %}
