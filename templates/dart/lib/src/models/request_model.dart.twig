{% macro sub_schema(property) %}{% if (property | schemaModel) %}{% if (property | schemaType) == 'array' %}List<{{(property | schemaModel) | caseUcfirst | overrideIdentifier}}>{% else %}{{(property | schemaModel) | caseUcfirst | overrideIdentifier }}{% endif %}{% else %}{% if (property | schemaType) == 'object' and property.additionalProperties %}Map<String, dynamic>{% else %}{{property | typeName}}{% endif %}{% endif %}{% endmacro %}
part of '../../models.dart';

/// {{ requestModel.description }}
class {{ requestModelName | caseUcfirst | overrideIdentifier }} implements Model {
{% for property in requestModel.properties %}
  /// {{ property.description }}
  final {% if not (property | schemaRequired) %}{{_self.sub_schema(property)}}? {{ (property | schemaName) | escapeKeyword }}{% else %}{{_self.sub_schema(property)}} {{ (property | schemaName) | escapeKeyword }}{% endif %};

{% endfor %}
  {{ requestModelName | caseUcfirst | overrideIdentifier }}({% if requestModel.properties | length %}{
{% else %}
{% endif %}{% for property in requestModel.properties %}
    {% if (property | schemaRequired) %}required {% endif %}this.{{ (property | schemaName) | escapeKeyword }},
{% endfor %}
{% if requestModel.properties | length %}  });
{% else %});
{% endif %}

  @override
  Map<String, dynamic> toMap() {
    return {
{% for property in requestModel.properties %}
      "{{ (property | schemaName) | escapeDollarSign }}": {% if (property | schemaModel) %}{% if (property | schemaType) == 'array' %}{{(property | schemaName) | escapeKeyword}}{% if not (property | schemaRequired) %}?{% endif %}.map((p) => p.toMap()).toList(){% else %}{{(property | schemaName) | escapeKeyword}}{% if not (property | schemaRequired) %}?{% endif %}.toMap(){% endif %}{% elseif (property | enumValues) is not empty %}{{(property | schemaName) | escapeKeyword}}{% if not (property | schemaRequired) %}?{% endif %}{% if (property | schemaType) == 'array' %}.map((item) => item.value).toList(){% else %}.value{% endif %}{% else %}{{(property | schemaName) | escapeKeyword }}{% endif %},
{% endfor %}
    };
  }
}
