import { Client } from "./src/client";
{% for service in spec.services %}
import { {{service.name | caseUcfirst}} } from "./src/services/{{service.name | caseDash}}";
{% endfor %}

export {
    Client,
{% for service in spec.services %}
    {{service.name | caseUcfirst}},
{% endfor %}
};