package {{ spec.namespace | caseDot }}.services;

import {{ spec.namespace | caseDot }}.Client;

abstract class Service {
    final Client client;

    Service(Client client) {
        this.client = client;
    }
}
