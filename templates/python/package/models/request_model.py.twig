from typing import Any, Dict, List, Optional, Union

from pydantic import Field

from .base_model import AppwriteModel
{% set added = [] %}
{%~ for property in requestModel.properties %}
{%~ set enumName = property | enumName %}
{%~ if (property | enumValues) is not empty %}
{%~ if enumName not in added %}
from ..enums.{{ enumName | caseSnake }} import {{ enumName | caseUcfirst }}
{%~ set added = added|merge([enumName]) %}
{%~ endif %}
{%~ endif %}
{%~ if (property | schemaModel) and (property | schemaModel) not in added %}
from .{{ (property | schemaModel) | caseSnake }} import {{ (property | schemaModel) | caseUcfirst }}
{%~ set added = added|merge([(property | schemaModel)]) %}
{%~ endif %}
{%~ if (property | schemaModels) is not empty %}
{%~ for subSchema in (property | schemaModels) %}
{%~ if subSchema not in added %}
from .{{ subSchema | caseSnake }} import {{ subSchema | caseUcfirst }}
{%~ set added = added|merge([subSchema]) %}
{%~ endif %}
{%~ endfor %}
{%~ endif %}
{%~ endfor %}


class {{ requestModelName | caseUcfirst }}(AppwriteModel):
    """
    {{ requestModel.description }}
{% if requestModel.properties | length > 0 %}

    Attributes
    ----------
{% for property in requestModel.properties %}
{% set propertyType %}{% if not (property | schemaRequired) and not (property | schemaNullable) %}Optional[{{ property | getModelPropertyType(requestModelName, spec) | raw }}]{% else %}{{ property | getModelPropertyType(requestModelName, spec) | raw }}{% endif %}{% endset %}
    {{ property | getModelFieldName(requestModel.properties) }} : {{ propertyType | trim }}
{% if property.description %}
        {{ property.description | formatDocstring(8) }}
{% else %}
        Typed model field.
{% endif %}
{% endfor %}
{% endif %}
    """

{% if requestModel.properties | length == 0 %}
    pass
{% else %}
{% for property in requestModel.properties %}
{% set fieldType %}{% if not (property | schemaRequired) and not (property | schemaNullable) %}Optional[{{ property | getModelPropertyType(requestModelName, spec) | raw }}]{% else %}{{ property | getModelPropertyType(requestModelName, spec) | raw }}{% endif %}{% endset %}
    {{ property | getModelFieldName(requestModel.properties) }}: {{ fieldType | trim | formatModelFieldType | raw }} = Field(
        {% if (property | schemaRequired) %}...{% else %}default=None{% endif %},
        alias='{{ (property | schemaName) }}',
    )
{% endfor %}
{% endif %}
