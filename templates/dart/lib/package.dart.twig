{% for service in spec.services %}
export 'package:{{ spec.title | caseDash }}/services/{{service.name | caseDash}}.dart';
{% endfor %}
export 'package:{{ spec.title | caseDash }}/client.dart';
export 'package:{{ spec.title | caseDash }}/enums.dart';
export 'package:dio/dio.dart' show Response;