class Permission {

    static read = (role) => {
         return `read("${role}")`
    }

    static write = (role) => {
         return `write("${role}")`
    }

    static create = (role) => {
         return `create("${role}")`
    }

    static update = (role) => {
         return `update("${role}")`
    }

    static delete = (role) => {
         return `delete("${role}")`
    }
}

module.exports = Permission;
