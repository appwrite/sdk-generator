<?php

namespace {{ spec.title | caseUcfirst }}\Enums;

class {{ enum.name | caseUcfirst | overrideIdentifier }}
{
{% for value in enum.enum %}
    public const {{value | caseUpper | overrideIdentifier}} = '{{value}}';
{% endfor %}
}
