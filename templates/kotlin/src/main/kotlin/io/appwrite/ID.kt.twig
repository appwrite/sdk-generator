package {{ sdk.namespace | caseDot }}

/**
 * Helper class for object IDs
 */
object ID {
    /**
     * Represents a custom ID
     *
     * @param id the custom ID
     */
    fun custom(id: String): String
            = id

    /**
     * Represents a randomly generated, unique ID
     */
    fun unique(): String
            = "unique()"
}
