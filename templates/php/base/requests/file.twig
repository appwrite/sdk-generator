{% set uploadIdParameter = method | uploadIdParameter %}
{% for parameter in (method | parameters('all')) %}
{% if (parameter | schemaType) == 'file' %}
{% set responseModels = method | getResponseModels(spec) %}
{% set hasResponseDiscriminator = (method | responseDiscriminator) is not empty %}
{% set responseDiscriminator = (method | responseDiscriminator) %}
        $size = 0;
        $mimeType = null;
        $postedName = null;
        if (empty(${{ parameter.name | caseCamel }}->getPath() ?? null)) {
            $size = strlen(${{ parameter.name | caseCamel }}->getData());
            $mimeType = ${{ parameter.name | caseCamel }}->getMimeType();
            $postedName = ${{ parameter.name | caseCamel }}->getFilename();
            if ($size <= Client::CHUNK_SIZE) {
                $apiParams['{{ parameter.name | caseCamel }}'] = Part::body('{{ parameter.name | caseCamel }}', ${{ parameter.name | caseCamel }}->getData(), $postedName, $mimeType);
                $response = $this->client->call(Client::METHOD_POST, $apiPath, [
    {% for param in (method | parameters('header')) %}
                    '{{ param.name }}' => ${{ param.name | caseCamel }},
    {% endfor %}
    {% for key, header in (method | methodHeaders) %}
                    '{{ key }}' => '{{ header }}',
    {% endfor %}
                    ], $apiParams);
{% if responseModels | length > 0 %}
{% if responseModels | length > 1 and hasResponseDiscriminator %}
                if (!is_array($response)) {
                    throw new \UnexpectedValueException('Expected array response when hydrating a response model.');
                }
{% for modelName, conditions in responseDiscriminator %}
                if ({% for field, expectedValue in conditions %}($response['{{ field }}'] ?? null) === '{{ expectedValue }}'{% if not loop.last %} && {% endif %}{% endfor %}) {
                    return \{{ namespace | caseNamespace }}\Models\{{ modelName | caseUcfirst }}::from($response);
                }
{% endfor %}
                throw new \UnexpectedValueException('Unable to match response to any expected response model.');
{% else %}
                if (!is_array($response)) {
                    throw new \UnexpectedValueException('Expected array response when hydrating a response model.');
                }

                return {{ responseModels | first }}::from($response);
{% endif %}
{% else %}
                return $response;
{% endif %}
            }
        } else {
            $size = filesize(${{ parameter.name | caseCamel }}->getPath());
            $mimeType = ${{ parameter.name | caseCamel }}->getMimeType() ?? mime_content_type(${{ parameter.name | caseCamel }}->getPath());
            $postedName = ${{ parameter.name | caseCamel }}->getFilename() ?? basename(${{ parameter.name | caseCamel }}->getPath());
            //send single file if size is less than or equal to 5MB
            if ($size <= Client::CHUNK_SIZE) {
                $apiParams['{{ parameter.name }}'] = Part::file('{{ parameter.name }}', ${{ parameter.name | caseCamel }}->getPath(), $postedName, $mimeType);
                $response = $this->client->call(Client::METHOD_{{ method.method.value | caseUpper }}, $apiPath, [
    {% for param in (method | parameters('header')) %}
                    '{{ param.name }}' => ${{ param.name | caseCamel }},
    {% endfor %}
    {% for key, header in (method | methodHeaders) %}
                    '{{ key }}' => '{{ header }}',
    {% endfor %}
                    ], $apiParams);
{% if responseModels | length > 0 %}
{% if responseModels | length > 1 and hasResponseDiscriminator %}
                if (!is_array($response)) {
                    throw new \UnexpectedValueException('Expected array response when hydrating a response model.');
                }
{% for modelName, conditions in responseDiscriminator %}
                if ({% for field, expectedValue in conditions %}($response['{{ field }}'] ?? null) === '{{ expectedValue }}'{% if not loop.last %} && {% endif %}{% endfor %}) {
                    return \{{ namespace | caseNamespace }}\Models\{{ modelName | caseUcfirst }}::from($response);
                }
{% endfor %}
                throw new \UnexpectedValueException('Unable to match response to any expected response model.');
{% else %}
                if (!is_array($response)) {
                    throw new \UnexpectedValueException('Expected array response when hydrating a response model.');
                }

                return {{ responseModels | first }}::from($response);
{% endif %}
{% else %}
                return $response;
{% endif %}
            }
        }

        $counter = 0;

{% if uploadIdParameter %}
        $response = null;
{% if uploadIdParameter.required %}
        try {
            $response = $this->client->call(Client::METHOD_GET, $apiPath . '/' . ${{ uploadIdParameter.name }});
            $counter = $response['chunksUploaded'] ?? 0;
        } catch (\Exception) {
        }
{% else %}
        if (!empty(${{ uploadIdParameter.name }})) {
            try {
                $response = $this->client->call(Client::METHOD_GET, $apiPath . '/' . ${{ uploadIdParameter.name }});
                $counter = $response['chunksUploaded'] ?? 0;
            } catch (\Exception) {
            }
        }
{% endif %}
{% endif %}

        $apiHeaders = [
        {%~ for key, security in (method | securityHeaders) %}
            '{{ security.name }}' => $this->client->getConfig('{{ key | caseLower }}'),
        {%~ endfor %}
        {%~ for parameter in (method | parameters('header')) %}
            '{{ parameter.name }}' => ${{ parameter.name | caseCamel }},
        {%~ endfor %}
        {%~ for key, header in (method | methodHeaders) %}
            '{{ key }}' => '{{ header }}',
        {%~ endfor %}
        ];
        $handle = null;

        if (!empty(${{parameter.name}}->getPath())) {
            $handle = @fopen(${{parameter.name}}->getPath(), "rb");
        }

{% if uploadIdParameter and uploadIdParameter.required %}
        $uploadId = ${{ uploadIdParameter.name }};
{% else %}
        $uploadId = '';
{% if uploadIdParameter %}
        $uploadId = ${{ uploadIdParameter.name }} ?? '';
{% endif %}
{% endif %}
        $totalChunks = (int) ceil($size / Client::CHUNK_SIZE);
        $chunks = [];
        $start = $counter * Client::CHUNK_SIZE;
        while ($start < $size) {
            $chunks[] = [
                'index' => $counter,
                'start' => $start,
                'end' => min($start + Client::CHUNK_SIZE, $size),
            ];
            $counter++;
            $start += Client::CHUNK_SIZE;
        }
{% if uploadIdParameter %}

        if ($chunks === []) {
{% if responseModels | length > 0 %}
{% if responseModels | length > 1 and hasResponseDiscriminator %}
            if (!is_array($response)) {
                throw new \UnexpectedValueException('Expected array response when hydrating a response model.');
            }
{% for modelName, conditions in responseDiscriminator %}
            if ({% for field, expectedValue in conditions %}($response['{{ field }}'] ?? null) === '{{ expectedValue }}'{% if not loop.last %} && {% endif %}{% endfor %}) {
                return \{{ namespace | caseNamespace }}\Models\{{ modelName | caseUcfirst }}::from($response);
            }
{% endfor %}
            throw new \UnexpectedValueException('Unable to match response to any expected response model.');
{% else %}
            if (!is_array($response)) {
                throw new \UnexpectedValueException('Expected array response when hydrating a response model.');
            }

            return {{ responseModels | first }}::from($response);
{% endif %}
{% else %}
            return $response;
{% endif %}
        }
{% endif %}

        $readChunk = function (int $start, int $end) use ($handle, ${{ parameter.name | caseCamel }}): string|false {
            if (!empty($handle)) {
                fseek($handle, $start);
                return @fread($handle, $end - $start);
            }

            return substr(${{ parameter.name | caseCamel }}->getData(), $start, $end - $start);
        };

        $uploadChunk = function (array $chunk, string $currentUploadId = '') use ($readChunk, $apiPath, $apiHeaders, $apiParams, $mimeType, $postedName, $size) {
            $chunkParams = $apiParams;
            $chunkHeaders = $apiHeaders;
            $data = $readChunk($chunk['start'], $chunk['end']);
            if (!is_string($data)) {
                throw new {{ namespace | split('\\') | last | caseUcfirst}}Exception('Failed to read upload chunk');
            }
            $chunkParams['{{ parameter.name }}'] = Part::body('{{ parameter.name }}', $data, $postedName, $mimeType);
            $chunkHeaders['content-range'] = 'bytes ' . $chunk['start'] . '-' . ($chunk['end'] - 1) . '/' . $size;
            if (!empty($currentUploadId)) {
                $chunkHeaders['x-{{spec.info.title | caseLower }}-id'] = $currentUploadId;
            }

            return $this->client->call(Client::METHOD_POST, $apiPath, $chunkHeaders, $chunkParams);
        };

        $isUploadComplete = function ($chunkResponse) use ($totalChunks): bool {
            if (!is_array($chunkResponse) || !isset($chunkResponse['chunksUploaded'])) {
                return false;
            }

            return (int) $chunkResponse['chunksUploaded'] >= (int) ($chunkResponse['chunksTotal'] ?? $totalChunks);
        };

        $response = $uploadChunk($chunks[0], $uploadId);
{% if not uploadIdParameter %}
        $uploadId = $response['$id'];
{% endif %}
        $completedCount = $chunks[0]['index'] + 1;
        $uploadedSize = $chunks[0]['end'];
        if ($onProgress !== null) {
            $onProgress([
                '$id' => $response['$id'],
                'progress' => $uploadedSize / $size * 100,
                'sizeUploaded' => $uploadedSize,
                'chunksTotal' => $totalChunks,
                'chunksUploaded' => $completedCount,
            ]);
        }

        $remainingChunks = array_slice($chunks, 1);
        $completedResponse = $isUploadComplete($response) ? $response : null;

        foreach ($remainingChunks as $chunk) {
            $response = $uploadChunk($chunk, $uploadId);
            $completedCount++;
            $uploadedSize += $chunk['end'] - $chunk['start'];
            if ($isUploadComplete($response)) {
                $completedResponse = $response;
            }
            if ($onProgress !== null) {
                $onProgress([
                    '$id' => $uploadId,
                    'progress' => $uploadedSize / $size * 100,
                    'sizeUploaded' => $uploadedSize,
                    'chunksTotal' => $totalChunks,
                    'chunksUploaded' => $completedCount,
                ]);
            }
        }

        $response = $completedResponse ?? $response;
        if (!empty($handle)) {
            @fclose($handle);
        }
{% if responseModels | length > 0 %}
{% if responseModels | length > 1 and hasResponseDiscriminator %}
        if (!is_array($response)) {
            throw new \UnexpectedValueException('Expected array response when hydrating a response model.');
        }
{% for modelName, conditions in responseDiscriminator %}
        if ({% for field, expectedValue in conditions %}($response['{{ field }}'] ?? null) === '{{ expectedValue }}'{% if not loop.last %} && {% endif %}{% endfor %}) {
            return \{{ namespace | caseNamespace }}\Models\{{ modelName | caseUcfirst }}::from($response);
        }
{% endfor %}
        throw new \UnexpectedValueException('Unable to match response to any expected response model.');
{% else %}
        if (!is_array($response)) {
            throw new \UnexpectedValueException('Expected array response when hydrating a response model.');
        }

        return {{ responseModels | first }}::from($response);
{% endif %}
{% else %}
        return $response;
{% endif %}
{% endif %}
{% endfor %}
