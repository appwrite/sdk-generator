import 'dart:convert';

/// {{spec.info.title | caseUcfirst}} Response
class Response<T> {
  /// Initializes a [Response]
  Response({this.data});

  /// HTTP body returned from {{spec.info.title | caseUcfirst}}
  T? data;

  @override
  String toString() {
    if (data is Map) {
      return json.encode(data);
    }
    return data.toString();
  }
}
