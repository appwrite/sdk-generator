        return try await client.call(
            method: "{{ method.method.value | caseUpper }}",
            path: apiPath,
            headers: apiHeaders,
            {%~ if (method | responseModel) %}
            params: apiParams,
            converter: converter
            {%~ else %}
            params: apiParams
            {%~ endif %}
        )