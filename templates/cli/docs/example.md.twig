```bash
{% set example = cliExample(method, service) %}
{% set exampleLines = example.arguments %}
{% if method | cliIsTopLevelAlias(service) %}
{{ language.params.executableName }} {{ (method | methodName) | caseKebab }}{% if exampleLines | length > 0 %} \{% endif %}
{% else %}
{{ language.params.executableName }} {{ service.name | caseLower }} {{ (method | methodName) | caseKebab }}{% if exampleLines | length > 0 %} \{% endif %}
{% endif %}

{% for line in exampleLines %}
    {{ line | raw }}{% if not loop.last %} \{% endif %}

{% endfor %}
```
{% if example.note %}

{{ example.note }}
{% endif %}
