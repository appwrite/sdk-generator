class_name {{ enum.name | caseUcfirst }}

{% for value in enum.enum %}
{% set key = enum.keys is empty ? value : enum.keys[loop.index0] %}
const {{ key | caseSnake | upper | escapeKeyword }} = "{{ value }}"
{% endfor %}
