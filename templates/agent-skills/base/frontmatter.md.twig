---
name: {{ spec.title | caseLower }}-{{ lang }}
description: {{ spec.title }} {{ langName }} SDK skill. Use this skill when building {{ langName }} applications with {{ spec.title }} — covers authentication, databases, storage, and all {{ spec.title }} API services.
license: {{ spec.licenseName }}
metadata:
  author: {{ sdk.gitUserName }}
  version: "{{ spec.version }}"
  organization: {{ spec.title }}
  abstract: Complete guide to using the {{ spec.title }} {{ langName }} SDK. Contains installation instructions, client initialization, and practical code examples for common operations including authentication, database CRUD, file storage, and real-time subscriptions.
---
