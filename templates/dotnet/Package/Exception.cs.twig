using System;

namespace {{spec.info.title | caseUcfirst}}
{
    public class {{spec.info.title | caseUcfirst}}Exception : Exception
    {
        public int? Code { get; set; }
        public string? Type { get; set; }
        public string? Response { get; set; }

        public {{spec.info.title | caseUcfirst}}Exception(
            string? message = null,
            int? code = null,
            string? type = null,
            string? response = null) : base(message)
        {
            this.Code = code;
            this.Type = type;
            this.Response = response;
        }

        public {{spec.info.title | caseUcfirst}}Exception(string message, Exception inner)
        : base(message, inner)
        {
        }
    }
}
