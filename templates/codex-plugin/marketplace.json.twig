{
    "name": "{{ spec.info.title | caseLower }}",
    "interface": {
        "displayName": "{{ spec.info.title }}"
    },
    "plugins": [
        {
            "name": "{{ spec.info.title | caseLower }}",
            "source": {
                "source": "local",
                "path": "./plugins/{{ spec.info.title | caseLower }}"
            },
            "policy": {
                "installation": "AVAILABLE",
                "authentication": "ON_INSTALL"
            },
            "category": "Developer Tools"
        }
    ]
}
