/// Exception thrown by the {{language.params.packageName}} package.
class {{spec.info.title | caseUcfirst}}Exception implements Exception {
  /// Error message.
  final String? message;

  /// Error type.
  ///
  /// See [Error Types]({{sdk.url}}/docs/response-codes#errorTypes)
  /// for more information.
  final String? type;
  final int? code;
  final String? response;

  /// Initializes an {{spec.info.title | caseUcfirst}} Exception.
  {{spec.info.title | caseUcfirst}}Exception([this.message = "", this.code, this.type, this.response]);

  /// Returns the error type, message, and code.
  @override
  String toString() {
    if (message == null || message == "") return "{{spec.info.title | caseUcfirst}}Exception";
    return "{{spec.info.title | caseUcfirst}}Exception: ${type ?? ''}, $message (${code ?? 0})";
  }
}
