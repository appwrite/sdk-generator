{
  "name": "{{ language.params.npmPackage }}",
  "homepage": "{{ spec.contactURL }}",
  "description": "{{ sdk.shortDescription }}",
  "version": "{{ sdk.version }}",
  "license": "{{ sdk.license }}",
  "exports": {
    "import": "./dist/mjs/sdk.js",
    "require": "./dist/cjs/sdk.js"
  },
  "repository": {
    "type": "git",
    "url": "{{ sdk.gitURL }}"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json && tsc -p tsconfig-cjs.json"
  },
  "devDependencies": {
    "typescript": "^4.2.3"
  },
  "dependencies": {}
}