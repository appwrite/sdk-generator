import Foundation

extension Query {

    public class Builder {
        private var queries: [String] = []

        public init() {}

        public func build() -> [String] {
            return queries
        }

        @discardableResult
        public func equal(_ attribute: String, value: Any) -> Builder {
            return push(Query.equal(attribute, value: value))
        }

        @discardableResult
        public func notEqual(_ attribute: String, value: Any) -> Builder {
            return push(Query.notEqual(attribute, value: value))
        }

        @discardableResult
        public func lessThan(_ attribute: String, value: Any) -> Builder {
            return push(Query.lessThan(attribute, value: value))
        }

        @discardableResult
        public func lessThanEqual(attribute: String, value: Any) -> Builder {
            return push(Query.lessThanEqual(attribute: attribute, value: value))
        }

        @discardableResult
        public func greaterThan(_ attribute: String, value: Any) -> Builder {
            return push(Query.greaterThan(attribute, value: value))
        }

        @discardableResult
        public func greaterThanEqual(_ attribute: String, value: Any) -> Builder {
            return push(Query.greaterThanEqual(attribute, value: value))
        }

        @discardableResult
        public func search(_ attribute: String, value: String) -> Builder {
            return push(Query.search(attribute, value: value))
        }

        @discardableResult
        public func isNull(_ attribute: String) -> Builder {
            return push(Query.isNull(attribute))
        }

        @discardableResult
        public func isNotNull(_ attribute: String) -> Builder {
            return push(Query.isNotNull(attribute))
        }

        @discardableResult
        public func between(_ attribute: String, start: Int, end: Int) -> Builder {
            return push(Query.between(attribute, start: start, end: end))
        }

        @discardableResult
        public func between(_ attribute: String, start: Double, end: Double) -> Builder {
            return push(Query.between(attribute, start: start, end: end))
        }

        @discardableResult
        public func between(_ attribute: String, start: String, end: String) -> Builder {
            return push(Query.between(attribute, start: start, end: end))
        }

        @discardableResult
        public func startsWith(_ attribute: String, value: String) -> Builder {
            return push(Query.startsWith(attribute, value: value))
        }

        @discardableResult
        public func endsWith(_ attribute: String, value: String) -> Builder {
            return push(Query.endsWith(attribute, value: value))
        }

        @discardableResult
        public func select(_ attributes: [String]) -> Builder {
            return push(Query.select(attributes))
        }

        @discardableResult
        public func cursorAfter(_ id: String) -> Builder {
            return push(Query.cursorAfter(id))
        }

        @discardableResult
        public func cursorBefore(_ id: String) -> Builder {
            return push(Query.cursorBefore(id))
        }

        @discardableResult
        public func orderAsc(_ attribute: String) -> Builder {
            return push(Query.orderAsc(attribute))
        }

        @discardableResult
        public func orderDesc(_ attribute: String) -> Builder {
            return push(Query.orderDesc(attribute))
        }

        @discardableResult
        public func orderRandom() -> Builder {
            return push(Query.orderRandom())
        }

        @discardableResult
        public func limit(_ limit: Int) -> Builder {
            return push(Query.limit(limit))
        }

        @discardableResult
        public func offset(_ offset: Int) -> Builder {
            return push(Query.offset(offset))
        }

        @discardableResult
        public func contains(_ attribute: String, value: Any) -> Builder {
            return push(Query.contains(attribute, value: value))
        }

        @discardableResult
        public func containsAny(_ attribute: String, value: [Any]) -> Builder {
            return push(Query.containsAny(attribute, value: value))
        }

        @discardableResult
        public func containsAll(_ attribute: String, value: [Any]) -> Builder {
            return push(Query.containsAll(attribute, value: value))
        }

        @discardableResult
        public func notContains(_ attribute: String, value: Any) -> Builder {
            return push(Query.notContains(attribute, value: value))
        }

        @discardableResult
        public func notSearch(_ attribute: String, value: String) -> Builder {
            return push(Query.notSearch(attribute, value: value))
        }

        @discardableResult
        public func notBetween(_ attribute: String, start: Int, end: Int) -> Builder {
            return push(Query.notBetween(attribute, start: start, end: end))
        }

        @discardableResult
        public func notBetween(_ attribute: String, start: Double, end: Double) -> Builder {
            return push(Query.notBetween(attribute, start: start, end: end))
        }

        @discardableResult
        public func notBetween(_ attribute: String, start: String, end: String) -> Builder {
            return push(Query.notBetween(attribute, start: start, end: end))
        }

        @discardableResult
        public func notStartsWith(_ attribute: String, value: String) -> Builder {
            return push(Query.notStartsWith(attribute, value: value))
        }

        @discardableResult
        public func notEndsWith(_ attribute: String, value: String) -> Builder {
            return push(Query.notEndsWith(attribute, value: value))
        }

        @discardableResult
        public func regex(_ attribute: String, pattern: String) -> Builder {
            return push(Query.regex(attribute, pattern: pattern))
        }

        @discardableResult
        public func exists(_ attributes: [String]) -> Builder {
            return push(Query.exists(attributes))
        }

        @discardableResult
        public func notExists(_ attributes: [String]) -> Builder {
            return push(Query.notExists(attributes))
        }

        @discardableResult
        public func createdBefore(_ value: String) -> Builder {
            return push(Query.createdBefore(value))
        }

        @discardableResult
        public func createdAfter(_ value: String) -> Builder {
            return push(Query.createdAfter(value))
        }

        @discardableResult
        public func createdBetween(_ start: String, _ end: String) -> Builder {
            return push(Query.createdBetween(start, end))
        }

        @discardableResult
        public func updatedBefore(_ value: String) -> Builder {
            return push(Query.updatedBefore(value))
        }

        @discardableResult
        public func updatedAfter(_ value: String) -> Builder {
            return push(Query.updatedAfter(value))
        }

        @discardableResult
        public func updatedBetween(_ start: String, _ end: String) -> Builder {
            return push(Query.updatedBetween(start, end))
        }

        @discardableResult
        public func or(_ queries: [String]) -> Builder {
            return push(Query.or(queries))
        }

        @discardableResult
        public func and(_ queries: [String]) -> Builder {
            return push(Query.and(queries))
        }

        @discardableResult
        public func elemMatch(_ attribute: String, queries: [String]) -> Builder {
            return push(Query.elemMatch(attribute, queries: queries))
        }

        @discardableResult
        public func distanceEqual(_ attribute: String, values: [Any], distance: Double, meters: Bool = true) -> Builder {
            return push(Query.distanceEqual(attribute, values: values, distance: distance, meters: meters))
        }

        @discardableResult
        public func distanceNotEqual(_ attribute: String, values: [Any], distance: Double, meters: Bool = true) -> Builder {
            return push(Query.distanceNotEqual(attribute, values: values, distance: distance, meters: meters))
        }

        @discardableResult
        public func distanceGreaterThan(_ attribute: String, values: [Any], distance: Double, meters: Bool = true) -> Builder {
            return push(Query.distanceGreaterThan(attribute, values: values, distance: distance, meters: meters))
        }

        @discardableResult
        public func distanceLessThan(_ attribute: String, values: [Any], distance: Double, meters: Bool = true) -> Builder {
            return push(Query.distanceLessThan(attribute, values: values, distance: distance, meters: meters))
        }

        @discardableResult
        public func intersects(_ attribute: String, values: [Any]) -> Builder {
            return push(Query.intersects(attribute, values: values))
        }

        @discardableResult
        public func notIntersects(_ attribute: String, values: [Any]) -> Builder {
            return push(Query.notIntersects(attribute, values: values))
        }

        @discardableResult
        public func crosses(_ attribute: String, values: [Any]) -> Builder {
            return push(Query.crosses(attribute, values: values))
        }

        @discardableResult
        public func notCrosses(_ attribute: String, values: [Any]) -> Builder {
            return push(Query.notCrosses(attribute, values: values))
        }

        @discardableResult
        public func overlaps(_ attribute: String, values: [Any]) -> Builder {
            return push(Query.overlaps(attribute, values: values))
        }

        @discardableResult
        public func notOverlaps(_ attribute: String, values: [Any]) -> Builder {
            return push(Query.notOverlaps(attribute, values: values))
        }

        @discardableResult
        public func touches(_ attribute: String, values: [Any]) -> Builder {
            return push(Query.touches(attribute, values: values))
        }

        @discardableResult
        public func notTouches(_ attribute: String, values: [Any]) -> Builder {
            return push(Query.notTouches(attribute, values: values))
        }

        @discardableResult
        public func count(_ attribute: String = "*", alias: String = "") -> Builder {
            return push(Query.count(attribute, alias: alias))
        }

        @discardableResult
        public func countDistinct(_ attribute: String, alias: String = "") -> Builder {
            return push(Query.countDistinct(attribute, alias: alias))
        }

        @discardableResult
        public func sum(_ attribute: String, alias: String = "") -> Builder {
            return push(Query.sum(attribute, alias: alias))
        }

        @discardableResult
        public func avg(_ attribute: String, alias: String = "") -> Builder {
            return push(Query.avg(attribute, alias: alias))
        }

        @discardableResult
        public func min(_ attribute: String, alias: String = "") -> Builder {
            return push(Query.min(attribute, alias: alias))
        }

        @discardableResult
        public func max(_ attribute: String, alias: String = "") -> Builder {
            return push(Query.max(attribute, alias: alias))
        }

        @discardableResult
        public func stddev(_ attribute: String, alias: String = "") -> Builder {
            return push(Query.stddev(attribute, alias: alias))
        }

        @discardableResult
        public func stddevPop(_ attribute: String, alias: String = "") -> Builder {
            return push(Query.stddevPop(attribute, alias: alias))
        }

        @discardableResult
        public func stddevSamp(_ attribute: String, alias: String = "") -> Builder {
            return push(Query.stddevSamp(attribute, alias: alias))
        }

        @discardableResult
        public func variance(_ attribute: String, alias: String = "") -> Builder {
            return push(Query.variance(attribute, alias: alias))
        }

        @discardableResult
        public func varPop(_ attribute: String, alias: String = "") -> Builder {
            return push(Query.varPop(attribute, alias: alias))
        }

        @discardableResult
        public func varSamp(_ attribute: String, alias: String = "") -> Builder {
            return push(Query.varSamp(attribute, alias: alias))
        }

        @discardableResult
        public func bitAnd(_ attribute: String, alias: String = "") -> Builder {
            return push(Query.bitAnd(attribute, alias: alias))
        }

        @discardableResult
        public func bitOr(_ attribute: String, alias: String = "") -> Builder {
            return push(Query.bitOr(attribute, alias: alias))
        }

        @discardableResult
        public func bitXor(_ attribute: String, alias: String = "") -> Builder {
            return push(Query.bitXor(attribute, alias: alias))
        }

        @discardableResult
        public func groupBy(_ attributes: [String]) -> Builder {
            return push(Query.groupBy(attributes))
        }

        @discardableResult
        public func having(_ queries: [String]) -> Builder {
            return push(Query.having(queries))
        }

        @discardableResult
        public func distinct() -> Builder {
            return push(Query.distinct())
        }

        @discardableResult
        public func join(_ table: String, left: String, right: String, operator op: String = "=", alias: String = "") -> Builder {
            return push(Query.join(table, left: left, right: right, operator: op, alias: alias))
        }

        @discardableResult
        public func join(_ table: String, on: [String]) -> Builder {
            return push(Query.join(table, on: on))
        }

        @discardableResult
        public func join(_ table: String, alias: String, on: [String]) -> Builder {
            return push(Query.join(table, alias: alias, on: on))
        }

        @discardableResult
        public func leftJoin(_ table: String, left: String, right: String, operator op: String = "=", alias: String = "") -> Builder {
            return push(Query.leftJoin(table, left: left, right: right, operator: op, alias: alias))
        }

        @discardableResult
        public func leftJoin(_ table: String, on: [String]) -> Builder {
            return push(Query.leftJoin(table, on: on))
        }

        @discardableResult
        public func leftJoin(_ table: String, alias: String, on: [String]) -> Builder {
            return push(Query.leftJoin(table, alias: alias, on: on))
        }

        @discardableResult
        public func rightJoin(_ table: String, left: String, right: String, operator op: String = "=", alias: String = "") -> Builder {
            return push(Query.rightJoin(table, left: left, right: right, operator: op, alias: alias))
        }

        @discardableResult
        public func rightJoin(_ table: String, on: [String]) -> Builder {
            return push(Query.rightJoin(table, on: on))
        }

        @discardableResult
        public func rightJoin(_ table: String, alias: String, on: [String]) -> Builder {
            return push(Query.rightJoin(table, alias: alias, on: on))
        }

        @discardableResult
        public func fullOuterJoin(_ table: String, left: String, right: String, operator op: String = "=", alias: String = "") -> Builder {
            return push(Query.fullOuterJoin(table, left: left, right: right, operator: op, alias: alias))
        }

        @discardableResult
        public func fullOuterJoin(_ table: String, on: [String]) -> Builder {
            return push(Query.fullOuterJoin(table, on: on))
        }

        @discardableResult
        public func fullOuterJoin(_ table: String, alias: String, on: [String]) -> Builder {
            return push(Query.fullOuterJoin(table, alias: alias, on: on))
        }

        @discardableResult
        public func crossJoin(_ table: String, alias: String = "") -> Builder {
            return push(Query.crossJoin(table, alias: alias))
        }

        @discardableResult
        public func vectorDot(_ attribute: String, vector: [Double]) -> Builder {
            return push(Query.vectorDot(attribute, vector: vector))
        }

        @discardableResult
        public func vectorCosine(_ attribute: String, vector: [Double]) -> Builder {
            return push(Query.vectorCosine(attribute, vector: vector))
        }

        @discardableResult
        public func vectorEuclidean(_ attribute: String, vector: [Double]) -> Builder {
            return push(Query.vectorEuclidean(attribute, vector: vector))
        }

        @discardableResult
        public func covers(_ attribute: String, values: [Any]) -> Builder {
            return push(Query.covers(attribute, values: values))
        }

        @discardableResult
        public func notCovers(_ attribute: String, values: [Any]) -> Builder {
            return push(Query.notCovers(attribute, values: values))
        }

        @discardableResult
        public func spatialEquals(_ attribute: String, values: [Any]) -> Builder {
            return push(Query.spatialEquals(attribute, values: values))
        }

        @discardableResult
        public func notSpatialEquals(_ attribute: String, values: [Any]) -> Builder {
            return push(Query.notSpatialEquals(attribute, values: values))
        }

        @discardableResult
        public func page(_ page: Int, perPage: Int = 25) -> Builder {
            return push(Query.page(page, perPage: perPage))
        }

        @discardableResult
        private func push(_ query: String) -> Builder {
            queries.append(query)
            return self
        }

        @discardableResult
        private func push(_ query: [String]) -> Builder {
            queries.append(contentsOf: query)
            return self
        }
    }
}
