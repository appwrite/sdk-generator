        return try await client.call(
            method: "{{ method.method | caseUpper }}",
            path: path,
            headers: headers,
            params: params{% if method.responseModel %},
            converter: converter
{% endif %}
        )