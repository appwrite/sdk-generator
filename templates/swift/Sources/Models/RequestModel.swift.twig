{% if requestModel.properties | filter(p => p | usesEnumType) | length > 0 %}
import {{spec.info.title | caseUcfirst}}Enums
{% endif %}
import Foundation
import JSONCodable

/// {{ requestModel.description }}
{% if requestModel.properties | length == 0 %}
open class {{ requestModelName | caseUcfirst }}: Codable {}
{% else %}
open class {{ requestModelName | caseUcfirst }}: Codable {

    enum CodingKeys: String, CodingKey {
        {%~ for property in requestModel.properties %}
        case {{ (property | schemaName) | escapeSwiftKeyword | removeDollarSign }} = "{{ (property | schemaName) }}"
        {%~ endfor %}
    }

    {%~ for property in requestModel.properties %}
    /// {{ property.description | trim }}
    public {% if (property | schemaRequired) %}let{% else %}var{% endif %} {{ (property | schemaName) | escapeSwiftKeyword | removeDollarSign }}: {{ property | propertyType(spec) | raw }}{% if not (property | schemaRequired) %}?{% endif %}

    {%~ endfor %}

    public init(
        {%~ for property in requestModel.properties %}
        {{ (property | schemaName) | escapeSwiftKeyword | removeDollarSign }}: {{ property | propertyType(spec) | raw  }}{% if not (property | schemaRequired) %}? = nil{% endif %}{% if not loop.last %},{% endif %}

        {%~ endfor %}
    ) {
        {%~ for property in requestModel.properties %}
        self.{{ (property | schemaName) | escapeSwiftKeyword | removeDollarSign }} = {{ (property | schemaName) | escapeSwiftKeyword | removeDollarSign }}
        {%~ endfor %}
    }

    public func toMap() -> [String: Any?] {
        return [
            {%~ for property in requestModel.properties %}
            "{{ (property | schemaName) }}": {% if (property | schemaModel) %}{% if (property | schemaType) == 'array' %}{{(property | schemaName) | escapeSwiftKeyword | removeDollarSign}}{% if not (property | schemaRequired) %}?{% endif %}.map { $0.toMap() }{% else %}{{(property | schemaName) | escapeSwiftKeyword | removeDollarSign}}{% if not (property | schemaRequired) %}?{% endif %}.toMap(){% endif %}{% elseif (property | schemaType) != 'array' and (property | enumValues) is not empty %}{{(property | schemaName) | escapeSwiftKeyword | removeDollarSign}}{% if not (property | schemaRequired) %}?{% endif %}.rawValue{% elseif (property | enumValues) %}{{(property | schemaName) | escapeSwiftKeyword | removeDollarSign}}{% if not (property | schemaRequired) %}?{% endif %}.map { $0.rawValue }{% else %}{{(property | schemaName) | escapeSwiftKeyword | removeDollarSign}}{% endif %}{% if not loop.last or requestModel.properties | length > 1 %},{% endif %}

            {%~ endfor %}
        ]
    }
}
{% endif %}
