        api_params = {}
{% if (method | parameters('all')) | length -%}
{% for parameter in (method | parameters('all')) -%}
{% if parameter.required %}
{%- set paramName = parameter.name | escapeKeyword | caseSnake -%}
{# Nullability permits None, not an empty string that collapses out of the path. Exact comparison preserves 0 and False. #}
{%- set emptyPath = parameter.location.value == 'path' and (parameter | schemaType) == 'string' and not (parameter | usesEnumType) %}
{% if not (parameter | schemaNullable) or emptyPath %}
        if {% if not (parameter | schemaNullable) %}{{ paramName }} is None{% if emptyPath %} or {% endif %}{% endif %}{% if emptyPath %}{{ paramName }} == ''{% endif %}:
            raise {{spec.info.title | caseUcfirst}}Exception('Missing required parameter: "{{ paramName }}"')
{% endif -%}
{% endif -%}
{% endfor -%}
{% for parameter in (method | parameters('path')) %}
        api_path = api_path.replace('{{ '{' }}{{ parameter.name }}{{ '}' }}', str(self._normalize_value({% if (parameter.extensions['x-sdk-source']|default('')) == 'security' %}self.client.get_config('{{ parameter.extensions['x-sdk-config'] | caseLower }}'){% else %}{{ parameter.name | escapeKeyword | caseSnake }}{% endif %})))
{% endfor -%}
{% for parameter in (method | parameters('query')) -%}
{% if not parameter.required %}
        if {{ parameter.name | escapeKeyword | caseSnake }} is not None:
{% set queryParam %}            api_params['{{ parameter.name }}'] = self._normalize_value({{ parameter.name | escapeKeyword | caseSnake }}){% endset %}
{{ queryParam | formatCall | raw }}
{% else %}
{% set queryParam %}        api_params['{{ parameter.name }}'] = self._normalize_value({{ parameter.name | escapeKeyword | caseSnake }}){% endset %}
{{ queryParam | formatCall | raw }}
{% endif -%}
{% endfor -%}
{% for parameter in (method | parameters('body'))|merge(method.parameters.formData|default([])) -%}
{% set paramName = parameter.name | escapeKeyword | caseSnake -%}
{% set isMultipart = (method | consumes)|length > 0 and (method | consumes)[0] == "multipart/form-data" -%}
{% set formattedValue = paramName | formatParamValue((parameter | schemaType), isMultipart) -%}
{% if not parameter.required %}
        if {{ paramName }} is not None:
{% set bodyParam %}            api_params['{{ parameter.name }}'] = self._normalize_value({{ formattedValue }}){% endset %}
{{ bodyParam | formatCall | raw }}
{% else %}
{% set bodyParam %}        api_params['{{ parameter.name }}'] = self._normalize_value({{ formattedValue }}){% endset %}
{{ bodyParam | formatCall | raw }}
{% endif -%}
{% endfor -%}
{% endif %}
