part of '../../enums.dart';

enum {{ enum.title | caseUcfirst | overrideIdentifier }} {
    {%~ for value in enum.enum %}
    {%~ set key = enum.extensions['x-enum-keys'] is empty ? value : enum.extensions['x-enum-keys'][loop.index0] %}
    {{ key | caseEnumKey | escapeKeyword }}(value: '{{ value }}'){% if not loop.last %},{% else %};{% endif %}

    {%~ endfor %}

    const {{ enum.title | caseUcfirst | overrideIdentifier }}({
        required this.value
    });

    final String value;

    String toJson() => value;
}