{% import _self as helper %}
{% macro mockParameterValue(parameter) -%}
{%- if parameter.enumName -%}
{%- if parameter.type == 'array' -%}
array({{ parameter.enumName | caseUcfirst }}::{{ (parameter.enumKeys[0] ?? parameter.enumValues[0]) | caseEnumKey }}())
{%- else -%}
{{ parameter.enumName | caseUcfirst }}::{{ (parameter.enumKeys[0] ?? parameter.enumValues[0]) | caseEnumKey }}()
{%- endif -%}
{%- elseif parameter.type == 'object' -%}
array()
{%- elseif parameter.type == 'array' -%}
array()
{%- elseif parameter.type == 'file' -%}
InputFile::withData('', 'image/png')
{%- elseif parameter.type == 'boolean' -%}
true
{%- elseif parameter.type == 'string' -%}
"{% if parameter.example is not empty %}{{ parameter.example | escapeDollarSign }}{% endif %}"
{%- elseif parameter.type == 'integer' and parameter['x-example'] is empty -%}
1
{%- elseif parameter.type == 'number' and parameter['x-example'] is empty -%}
1.0
{%- else -%}
{{ parameter.example }}
{%- endif -%}
{%- endmacro %}
<?php

namespace {{ spec.namespace | caseNamespace }}\Services;

use {{ spec.namespace | caseNamespace }}\Client;
use {{ spec.namespace | caseNamespace }}\InputFile;
use Mockery;
use PHPUnit\Framework\TestCase;
{% set added = [] %}
{% for method in service.methods %}
{% for parameter in method.parameters.all %}
{% if parameter.enumName is not empty and parameter.enumName not in added %}
use {{ spec.namespace | caseNamespace }}\Enums\{{ parameter.enumName | caseUcfirst }};
{% set added = added|merge([parameter.enumName]) %}
{% endif %}
{% endfor %}
{% endfor %}

final class {{ service.name | caseUcfirst }}Test extends TestCase
{
    private $client;
    private ${{ service.name | caseCamel }};

    protected function setUp(): void
    {
        $this->client = Mockery::mock(Client::class);
        $this->{{ service.name | caseCamel }} = new {{ service.name | caseUcfirst }}($this->client);
    }

{% set nonDeprecatedMethodNames = [] %}
{% for method in service.methods %}
{% if not method.deprecated %}
{% set nonDeprecatedMethodNames = nonDeprecatedMethodNames|merge([method.name | caseCamel | lower]) %}
{% endif %}
{% endfor %}
{% for method in service.methods %}
{% set methodNameLower = method.name | caseCamel | lower %}
{% if method.deprecated and methodNameLower in nonDeprecatedMethodNames %}
{# Skip deprecated methods that have namespace collisions with non-deprecated methods #}
{% else %}
{% set responseModels = method | getResponseModels(spec) %}
{% set filteredResponseModelNames = method | getValidResponseModels %}
{% set responseDefinitionName = '' %}
{% if filteredResponseModelNames is not empty %}
{% set responseDefinitionName = filteredResponseModelNames | last %}
{% elseif method.responseModel and method.responseModel != 'any' %}
{% set responseDefinitionName = method.responseModel %}
{% endif %}
{% set requiredParameters = method.parameters.all | filter((param) => param.required) %}
    public function testMethod{{ method.name | caseUcfirst }}(): void
    {
{% if responseModels | length > 0 %}
        $data = {{ responseDefinitionName | mockDefinitionPayload(spec, 3) | raw -}};
{% elseif (method.responseModel and method.responseModel == 'any') or (method | getReturn(spec) == 'array') %}
        $data = array();
{% else %}
        $data = '';
{% endif %}

        $this->client
            ->allows()->call(Mockery::any(), Mockery::any(), Mockery::any(), Mockery::any(){% if method.type == 'webAuth' %}, Mockery::any(){% endif %})
            ->andReturn($data);

{% if requiredParameters | length > 0 %}
        $response = $this->{{ service.name | caseCamel }}->{{ method.name | caseCamel }}(
{% for parameter in requiredParameters %}
            {{ helper.mockParameterValue(parameter) }}{{ loop.last ? '' : ',' }}
{% endfor %}
        );
{% else %}
        $response = $this->{{ service.name | caseCamel }}->{{ method.name | caseCamel }}();
{% endif %}

{% if responseModels | length > 0 %}
        $this->assertInstanceOf({{ responseModels | last }}::class, $response);
{% else %}
        $this->assertSame($data, $response);
{% endif %}
    }

{% endif %}
{% endfor %}
}
