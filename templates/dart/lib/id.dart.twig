part of {{ language.params.packageName }};

class ID {
    const ID._();
    
    static String unique() {
        return 'unique()';
    }

    static String custom(String id) {
        return id;
    }
}