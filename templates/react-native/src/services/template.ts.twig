{% set usesUpload = false %}
{% set usesModels = false %}
{% set usesException = false %}
{% for method in (service | operations) %}
{% if 'multipart/form-data' in (method | consumes) %}{% set usesUpload = true %}{% endif %}
{% if 'Models.' in (method | getReturn(spec)) or 'Models.' in ((method | responseModel) | getGenerics(spec)) %}{% set usesModels = true %}{% endif %}
{% for parameter in (method | parameters('all')) %}
{% if parameter.required %}{% set usesException = true %}{% endif %}
{% if 'Models.' in (parameter | getPropertyType(method, spec)) %}{% set usesModels = true %}{% endif %}
{% endfor %}
{% endfor %}
{% set clientImports = [] %}
{% if usesException %}{% set clientImports = clientImports | merge([(spec.info.title | caseUcfirst) ~ 'Exception']) %}{% endif %}
{% set clientImports = clientImports | merge(['Client']) %}
import { Service } from '../service';
{{ clientImports | jsImport('../client') | raw }}
{% if usesModels %}
import type { Models } from '../models';
{% endif %}
{% set typeImports = [] %}
{% if usesUpload %}{% set typeImports = typeImports | merge(['UploadProgress']) %}{% endif %}
{% set typeImports = typeImports | merge(['Payload']) %}
import type { {{ typeImports | join(', ') }} } from '../client';
{% if usesUpload %}
import * as FileSystem from 'expo-file-system';
import { Platform as RNPlatform } from 'react-native';
{% endif %}

{% set added = [] %}
{% for method in (service | operations) %}
{% for parameter in (method | parameters('all')) %}
{% if (parameter | enumValues) is not empty %}
{% if (parameter | enumName) not in added %}
import { {{ (parameter | enumName) | caseUcfirst }} } from '../enums/{{ (parameter | enumName) | caseKebab }}';
{% set added = added|merge([(parameter | enumName)]) %}
{% endif %}
{% endif %}
{% endfor %}
{% endfor %}
{#- #}
export class {{ service.name | caseUcfirst }} extends Service {
    constructor(client: Client) {
        super(client);
    }
    {%~ for method in (service | operations) %}

    /**
    {%~ if method.description %}
     * {{ method.description | replace({'\n': '\n     * '}) | trimLines | raw }}
    {%~ endif %}
     *
    {%~ for parameter in (method | parameters('all')) %}
     * @param {{ '{' }}{{ parameter | getPropertyType(method, spec) | raw }}{{ '}' }} params.{{ parameter.name | caseCamel | escapeKeyword }} - {{ parameter.description | raw }}
    {%~ endfor %}
     * @throws {{ '{' }}{{ spec.info.title | caseUcfirst}}Exception}
     * @returns {% if (method | methodType) == 'webAuth' %}{void|string}{% elseif (method | methodType) == 'location' %}{ArrayBuffer}{% else %}{Promise}{% endif %}

    {%~ if method.deprecated %}
    {%~ if (method | appwrite('deprecated', {})['since'] ?? '') and (method | appwrite('deprecated', {})['replaceWith'] ?? '') %}
     * @deprecated This API has been deprecated since {{ (method | appwrite('deprecated', {})['since'] ?? '') }}. Please use `{{ (method | appwrite('deprecated', {})['replaceWith'] ?? '') | capitalizeFirst }}` instead.
    {%~ else %}
     * @deprecated This API has been deprecated.
    {%~ endif %}
    {%~ endif %}
     */
     {%~ if (method | parameters('all'))|length > 0 %}
{% set hasRequiredParams = false %}{% for parameter in (method | parameters('all')) %}{% if parameter.required %}{% set hasRequiredParams = true %}{% endif %}{% endfor %}{% set objectParam %}params{% if not hasRequiredParams %}?{% endif %}: { {% for parameter in (method | parameters('all')) %}{{ parameter.name | caseCamel | escapeKeyword }}{% if not parameter.required or (parameter | schemaNullable) %}?{% endif %}: {{ parameter | getPropertyType(method, spec) | raw }}{% if not loop.last %}; {% endif %}{% endfor %}{% if 'multipart/form-data' in (method | consumes) %}; onProgress?: (progress: UploadProgress) => void{% endif %} }{% endset %}
    {% if 'multipart/form-data' in (method | consumes) %}async {% endif %}{{ ((method | methodName) | caseCamel ~ ((method | responseModel) | getGenerics(spec))) | tsSignature([objectParam], (method | getReturn(spec)), 4, ';') | raw }}
    /**
     {%~ if method.description %}
     * {{ method.description | replace({'\n': '\n     * '}) | trimLines | raw }}
     {%~ endif %}
     *
     {%~ for parameter in (method | parameters('all')) %}
     * @param {{ '{' }}{{ parameter | getPropertyType(method, spec) | raw }}{{ '}' }} {{ parameter.name | caseCamel | escapeKeyword }} - {{ parameter.description | raw }}
     {%~ endfor %}
     * @throws {{ '{' }}{{ spec.info.title | caseUcfirst}}Exception}
     * @returns {{ '{' }}{{ method | getReturn(spec) | raw }}{{ '}' }}
     * @deprecated Use the object parameter style method for a better developer experience.
     */
{% set positional = [] %}{% for parameter in (method | parameters('all')) %}{% set positional = positional|merge([(parameter.name | caseCamel | escapeKeyword) ~ ((not parameter.required or (parameter | schemaNullable)) ? '?' : '') ~ ': ' ~ (parameter | getPropertyType(method, spec))]) %}{% endfor %}{% if 'multipart/form-data' in (method | consumes) %}{% set positional = positional|merge(['onProgress?: (progress: UploadProgress) => void']) %}{% endif %}
    {% if 'multipart/form-data' in (method | consumes) %}async {% endif %}{{ ((method | methodName) | caseCamel ~ ((method | responseModel) | getGenerics(spec))) | tsSignature(positional, (method | getReturn(spec)), 4, ';') | raw }}
{% set implParams = [] -%}
{%- set allParams = method | parameters('all') -%}
{%- if allParams|length > 0 -%}
    {%- set firstParam = allParams[0] -%}
    {%- set firstOptional = not firstParam.required or (firstParam | schemaNullable) -%}
    {%- set objectType %}{ {% for parameter in allParams %}{{ parameter.name | caseCamel | escapeKeyword }}{% if not parameter.required or (parameter | schemaNullable) %}?{% endif %}: {{ parameter | getPropertyType(method, spec) | raw }}{% if not loop.last %}; {% endif %}{% endfor %}{% if 'multipart/form-data' in (method | consumes) %}; onProgress?: (progress: UploadProgress) => void{% endif %} }{% endset -%}
    {%- set firstType = firstParam | getPropertyType(method, spec) -%}
    {%- set implParams = implParams|merge(['paramsOrFirst' ~ (firstOptional ? '?' : '') ~ ': ' ~ objectType ~ ' | ' ~ firstType]) -%}
    {%- if allParams|length > 1 -%}
        {%- set restTuple %}...rest: [{% for parameter in allParams[1:] %}{{ (parameter | getPropertyType(method, spec)) | tsTupleElement | raw }}{% if not loop.last %}, {% endif %}{% endfor %}{% if 'multipart/form-data' in (method | consumes) %}, ((progress: UploadProgress) => void)?{% endif %}]{% endset -%}
        {%- set implParams = implParams|merge([restTuple]) -%}
    {%- endif -%}
{%- endif %}
    {% if 'multipart/form-data' in (method | consumes) %}async {% endif %}{{ ((method | methodName) | caseCamel ~ ((method | responseModel) | getGenerics(spec))) | tsSignature(implParams, (method | getReturn(spec)), 4, ' {') | raw }}
        {%~ if (method | parameters('all'))|length > 0 %}
{% set membersBody %}{% for parameter in (method | parameters('all')) %}{{ parameter.name | caseCamel | escapeKeyword }}{% if not parameter.required or (parameter | schemaNullable) %}?{% endif %}: {{ parameter | getPropertyType(method, spec) | raw }}{% if not loop.last %}; {% endif %}{% endfor %}{% endset %}
        {{ 'let params: ' | tsObjectStatement(membersBody, ';', 8) | raw }}
        {%~ if 'multipart/form-data' in (method | consumes) %}
        let onProgress: (progress: UploadProgress) => void;
        {%~ endif %}

        if (
            {{ method | getOverloadCondition(spec) | raw }}
        ) {
            {{ 'params = (paramsOrFirst || {}) as ' | tsObjectStatement(membersBody, ';', 12) | raw }}
            {%~ if 'multipart/form-data' in (method | consumes) %}
            onProgress = paramsOrFirst?.onProgress as (
                progress: UploadProgress,
            ) => void;
            {%~ endif %}
        } else {
            params = {
                {%~ for parameter in (method | parameters('all')) %}
                {{ ((parameter.name | caseCamel | escapeKeyword) ~ ': ' ~ (loop.index0 == 0 ? 'paramsOrFirst' : ('rest[' ~ (loop.index0 - 1) ~ ']')) ~ ' as') | tsAssignment((parameter | getPropertyType(method, spec)), 16) | raw }},
                {%~ endfor %}
            };
            {%~ if 'multipart/form-data' in (method | consumes) %}
            onProgress = rest[{{ (method | parameters('all'))|length - 1 }}] as (progress: UploadProgress) => void;
            {%~ endif %}
        }

        {%~ for parameter in (method | parameters('all')) %}
        {{ ('const ' ~ (parameter.name | caseCamel | escapeKeyword)) | jsAssign(('params.' ~ (parameter.name | caseCamel | escapeKeyword)), 8) | raw }}
        {%~ endfor %}

        {%~ else %}
        {%~ if 'multipart/form-data' in (method | consumes) %}
        if (typeof paramsOrFirst === 'function') {
            onProgress = paramsOrFirst;
        }
        {%~ endif %}
        {%~ endif %}
    {%~ else %}
{% set bareParams = [] %}{% for parameter in (method | parameters('all')) %}{% set bareParams = bareParams|merge([(parameter.name | caseCamel | escapeKeyword) ~ ((not parameter.required or (parameter | schemaNullable)) ? '?' : '') ~ ': ' ~ (parameter | getPropertyType(method, spec))]) %}{% endfor %}{% if 'multipart/form-data' in (method | consumes) %}{% set bareParams = bareParams|merge(['onProgress = (progress: UploadProgress) => void']) %}{% endif %}
    {{ ((method | methodName) | caseCamel ~ ((method | responseModel) | getGenerics(spec))) | tsSignature(bareParams, (method | getReturn(spec)), 4, ' {') | raw }}
    {%~ endif %}
{% for parameter in (method | parameters('all')) %}
{% if parameter.required %}
        if (typeof {{ parameter.name | caseCamel | escapeKeyword }} === 'undefined') {
            {{ ('throw new ' ~ (spec.info.title | caseUcfirst) ~ 'Exception') | tsCall('\'Missing required parameter: "' ~ (parameter.name | caseCamel | escapeKeyword) ~ '"\'', ';', 12) | raw }}
        }

{% endif %}
{% endfor %}
{% set pathReplacements = [] %}{% for parameter in (method | parameters('path')) %}{% set pathReplacements = pathReplacements|merge([['{' ~ parameter.name ~ '}', 'encodeURIComponent(String(' ~ (((parameter.extensions['x-sdk-source']|default('')) == 'security') ? ('this.client.config.' ~ (parameter.extensions['x-sdk-config'] | caseLower)) : (parameter.name | caseCamel | escapeKeyword)) ~ '))']]) %}{% endfor %}
        {{ method.path | tsApiPath(pathReplacements, 8) | raw }}
        const payload: Payload = {};

{% for parameter in (method | parameters('query')) %}
        if (typeof {{ parameter.name | caseCamel | escapeKeyword }} !== 'undefined') {
            {{ ("payload['" ~ parameter.name ~ "']") | jsAssign((parameter.name | caseCamel | escapeKeyword), 12) | raw }}
        }

{% endfor %}
{% for parameter in (method | parameters('body')) %}
        if (typeof {{ parameter.name | caseCamel | escapeKeyword }} !== 'undefined') {
            {{ ("payload['" ~ parameter.name ~ "']") | jsAssign((parameter.name | caseCamel | escapeKeyword), 12) | raw }}
        }

{% endfor %}
        const uri = new URL(this.client.config.endpoint + apiPath);
        {%~ for security in (method | securityQueries) %}
        uri.searchParams.append('{{ security.name }}', this.client.config.{{ security.name | caseLower }});
        {%~ endfor %}
{% if (method | methodType) == 'location' or (method | methodType) == 'webAuth' %}
{% if [(method | securitySchemes)]|length > 0 %}
{% for node in [(method | securitySchemes)] %}
{% for key,header in node|keys %}
        payload['{{header|caseLower}}'] = this.client.config.{{header|caseLower}};

{% endfor %}
{% endfor %}
{% endif %}
        for (const [key, value] of Object.entries(Service.flatten(payload))) {
            uri.searchParams.append(key, value);
        }
{% endif %}
{% if (method | methodType) == 'webAuth' %}
        return uri;
{% else %}
{% if 'multipart/form-data' in (method | consumes) %}
{% for parameter in (method | parameters('all')) %}
{% if (parameter | schemaType) == 'file' %}

        const size = {{ parameter.name | caseCamel | escapeKeyword }}.size;

        if (size <= Service.CHUNK_SIZE) {
            return this.client.call(
                '{{ method.method.value | caseLower }}',
                uri,
                {
                {%~ for key, security in (method | securityHeaders) %}
                    {{ security.name | jsKey | raw }}: this.client.config.{{ key | caseLower }},
                {%~ endfor %}
                {%~ for parameter in (method | parameters('header')) %}
                    {{ (parameter.name | caseCamel | escapeKeyword) | jsKey | raw }}: this.client.${{ parameter.name | caseCamel | escapeKeyword }},
                {%~ endfor %}
                {%~ for key, header in (method | methodHeaders) %}
                    {{ key | jsKey | raw }}: '{{ header }}',
                {%~ endfor %}
                },
                payload,
            );
        }

        const apiHeaders: { [header: string]: string } = {
        {%~ for key, security in (method | securityHeaders) %}
            {{ security.name | jsKey | raw }}: this.client.config.{{ key | caseLower }},
        {%~ endfor %}
        {%~ for parameter in (method | parameters('header')) %}
            {{ (parameter.name | caseCamel | escapeKeyword) | jsKey | raw }}: this.client.${{ parameter.name | caseCamel | escapeKeyword }},
        {%~ endfor %}
        {%~ for key, header in (method | methodHeaders) %}
            {{ key | jsKey | raw }}: '{{ header }}',
        {%~ endfor %}
        };

        let offset = 0;
        let response = undefined;
{% set uploadIdParameter = method | uploadIdParameter %}
{% if uploadIdParameter %}
        const providedUploadId = {{ uploadIdParameter.name | caseCamel | escapeKeyword }};
        if (providedUploadId) {
            try {
                response = await this.client.call(
                    'GET',
                    new URL(
                        this.client.config.endpoint +
                            apiPath +
                            '/' +
                            providedUploadId,
                    ),
                    apiHeaders,
                );
                offset = response.chunksUploaded * Service.CHUNK_SIZE;
            } catch (_e) {
                // A missing upload simply means we start from the first chunk.
            }
        }
{% endif %}

        const totalChunks = Math.ceil(size / Service.CHUNK_SIZE);

        // Upload first chunk alone to get the upload ID
        if (offset === 0) {
            const firstChunkEnd = Math.min(Service.CHUNK_SIZE, size);
            const firstChunkHeaders = {
                ...apiHeaders,
                'content-range': 'bytes 0-' + (firstChunkEnd - 1) + '/' + size,
            };

            const firstChunk = await FileSystem.readAsStringAsync({{ parameter.name | caseCamel | escapeKeyword }}.uri, {
                encoding: FileSystem.EncodingType.Base64,
                position: 0,
                length: Service.CHUNK_SIZE,
            });
            let firstPath = `data:${{'{'}}{{ parameter.name | caseCamel | escapeKeyword }}.type{{'}'}};base64,${{'{'}}firstChunk{{'}'}}`;
            if (RNPlatform.OS.toLowerCase() === 'android') {
                firstPath =
                    FileSystem.cacheDirectory +
                    '/tmp_chunk_' +
                    new Date().getTime();
                await FileSystem.writeAsStringAsync(firstPath, firstChunk, {
                    encoding: FileSystem.EncodingType.Base64,
                });
            }

            payload['{{ parameter.name }}'] = {{ '{' }}
                uri: firstPath,
                name: {{ parameter.name | caseCamel | escapeKeyword }}.name,
                type: {{ parameter.name | caseCamel | escapeKeyword }}.type,
            };

            response = await this.client.call(
                '{{ method.method.value | caseLower }}',
                uri,
                firstChunkHeaders,
                payload,
            );
            offset = firstChunkEnd;

            if (onProgress) {
                onProgress({
                    $id: response.$id,
                    progress: (offset / size) * 100,
                    sizeUploaded: offset,
                    chunksTotal: totalChunks,
                    chunksUploaded: 1,
                });
            }
        }

        if (offset >= size) {
            return response;
        }

        const uploadId = response?.$id;
        const chunks: { index: number; start: number; end: number }[] = [];
        const startChunkIndex = Math.ceil(offset / Service.CHUNK_SIZE);
        for (let i = startChunkIndex; i < totalChunks; i++) {
            const start = i * Service.CHUNK_SIZE;
            const end = Math.min(start + Service.CHUNK_SIZE, size);
            chunks.push({ index: i, start, end });
        }

        // Upload remaining chunks with max concurrency of 8
        const CONCURRENCY = 8;
        let completedCount = startChunkIndex;
        let uploadedBytes = offset;
        let finalResponse = null;
        let failed = false;

        const isUploadComplete = (chunkResponse: any) => {
            const chunksUploaded = chunkResponse?.chunksUploaded;
            const chunksTotal = chunkResponse?.chunksTotal ?? totalChunks;
            return (
                typeof chunksUploaded === 'number' &&
                typeof chunksTotal === 'number' &&
                chunksUploaded >= chunksTotal
            );
        };

        const uploadChunk = async (chunk: (typeof chunks)[0]) => {
            const chunkHeaders = { ...apiHeaders };
            if (uploadId) {
                chunkHeaders['x-{{spec.info.title | caseLower }}-id'] = uploadId;
            }
            chunkHeaders['content-range'] =
                'bytes ' + chunk.start + '-' + (chunk.end - 1) + '/' + size;

            const chunkData = await FileSystem.readAsStringAsync({{ parameter.name | caseCamel | escapeKeyword }}.uri, {
                encoding: FileSystem.EncodingType.Base64,
                position: chunk.start,
                length: chunk.end - chunk.start,
            });

            let chunkPath = `data:${{'{'}}{{ parameter.name | caseCamel | escapeKeyword }}.type{{'}'}};base64,${{'{'}}chunkData{{'}'}}`;
            if (RNPlatform.OS.toLowerCase() === 'android') {
                chunkPath =
                    FileSystem.cacheDirectory +
                    '/tmp_chunk_' +
                    new Date().getTime() +
                    '_' +
                    chunk.index;
                await FileSystem.writeAsStringAsync(chunkPath, chunkData, {
                    encoding: FileSystem.EncodingType.Base64,
                });
            }

            const chunkPayload = { ...payload };
            chunkPayload['{{ parameter.name }}'] = {{ '{' }}
                uri: chunkPath,
                name: {{ parameter.name | caseCamel | escapeKeyword }}.name,
                type: {{ parameter.name | caseCamel | escapeKeyword }}.type,
            };

            const chunkResponse = await this.client.call(
                '{{ method.method.value | caseLower }}',
                uri,
                chunkHeaders,
                chunkPayload,
            );

            if (failed) {
                return chunkResponse;
            }

            completedCount++;
            uploadedBytes += chunk.end - chunk.start;

            response = chunkResponse;
            if (isUploadComplete(chunkResponse)) {
                finalResponse = chunkResponse;
            }

            if (onProgress) {
                onProgress({
                    $id: uploadId,
                    progress: (uploadedBytes / size) * 100,
                    sizeUploaded: uploadedBytes,
                    chunksTotal: totalChunks,
                    chunksUploaded: completedCount,
                });
            }

            return chunkResponse;
        };

        // Process with limited concurrency using a worker pool
        const queue = [...chunks];
        const workers: Promise<void>[] = [];
        const workerCount = Math.min(CONCURRENCY, queue.length);

        for (let i = 0; i < workerCount; i++) {
            workers.push(
                (async () => {
                    while (!failed && queue.length > 0) {
                        const chunk = queue.shift()!;
                        try {
                            await uploadChunk(chunk);
                        } catch (error) {
                            failed = true;
                            throw error;
                        }
                    }
                })(),
            );
        }

        await Promise.all(workers);

        return finalResponse ?? response;
{% endif %}
{% endfor %}
{% else %}
        return this.client.call(
            '{{ method.method.value | caseLower }}',
            uri,
            {
            {%~ for key, security in (method | securityHeaders) %}
                {{ security.name | jsKey | raw }}: this.client.config.{{ key | caseLower }},
            {%~ endfor %}
            {%~ for parameter in (method | parameters('header')) %}
                {{ (parameter.name | caseCamel | escapeKeyword) | jsKey | raw }}: this.client.${{ parameter.name | caseCamel | escapeKeyword }},
            {%~ endfor %}
            {%~ for key, header in (method | methodHeaders) %}
                {{ key | jsKey | raw }}: '{{ header }}',
            {%~ endfor %}
            },
            payload{% if (method | methodType) == 'location' %},
            'arrayBuffer'{% endif %},
        );
{% endif %}
{% endif %}
    }
{% endfor %}
{# Extra methods for just getting the URL of 'location' type methods #}
{% for method in (service | operations) %}
{% if (method | methodType) == 'location' %}

    /**
{% if method.description %}
{{ (method.description | wrap(75, '     * ')) | trimLines }}
{% endif %}
     *
{% for parameter in (method | parameters('all')) %}
     * @param {{ '{' }}{{ parameter | getPropertyType(method, spec) | raw }}{{ '}' }} {{ parameter.name | caseCamel | escapeKeyword }}
{% endfor %}
     * @throws {{ '{' }}{{ spec.info.title | caseUcfirst}}Exception}
     * @returns {{ '{' }}URL{{ '}' }}
    {%~ if method.deprecated %}
     * @deprecated This API has been deprecated since {{ (method | appwrite('deprecated', {})['since'] ?? '') }}. Please use `{{ (method | appwrite('deprecated', {})['replaceWith'] ?? '') | capitalizeFirst }}` instead.
    {%~ endif %}
     */
{% set bareParams = [] %}{% for parameter in (method | parameters('all')) %}{% set bareParams = bareParams|merge([(parameter.name | caseCamel | escapeKeyword) ~ ((not parameter.required or (parameter | schemaNullable)) ? '?' : '') ~ ': ' ~ (parameter | getPropertyType(method, spec))]) %}{% endfor %}{% if 'multipart/form-data' in (method | consumes) %}{% set bareParams = bareParams|merge(['onProgress = (progress: UploadProgress) => void']) %}{% endif %}
    {{ ((method | methodName) | caseCamel ~ 'URL') | tsSignature(bareParams, 'URL', 4, ' {') | raw }}
{% set pathReplacements = [] %}{% for parameter in (method | parameters('path')) %}{% set pathReplacements = pathReplacements|merge([['{' ~ parameter.name ~ '}', 'encodeURIComponent(String(' ~ (((parameter.extensions['x-sdk-source']|default('')) == 'security') ? ('this.client.config.' ~ (parameter.extensions['x-sdk-config'] | caseLower)) : (parameter.name | caseCamel | escapeKeyword)) ~ '))']]) %}{% endfor %}
        {{ method.path | tsApiPath(pathReplacements, 8) | raw }}
        const payload: Payload = {};

{% for parameter in (method | parameters('query')) %}
        if (typeof {{ parameter.name | caseCamel | escapeKeyword }} !== 'undefined') {
            {{ ("payload['" ~ parameter.name ~ "']") | jsAssign((parameter.name | caseCamel | escapeKeyword), 12) | raw }}
        }

{% endfor %}
{% for parameter in (method | parameters('body')) %}
        if (typeof {{ parameter.name | caseCamel | escapeKeyword }} !== 'undefined') {
            {{ ("payload['" ~ parameter.name ~ "']") | jsAssign((parameter.name | caseCamel | escapeKeyword), 12) | raw }}
        }

{% endfor %}
        const uri = new URL(this.client.config.endpoint + apiPath);
        {%~ for security in (method | securityQueries) %}
        uri.searchParams.append('{{ security.name }}', this.client.config.{{ security.name | caseLower }});
        {%~ endfor %}
{% for node in [(method | securitySchemes)] %}
{% for key,header in node|keys %}
        payload['{{header|caseLower}}'] = this.client.config.{{header|caseLower}};

{% endfor %}
{% endfor %}
        for (const [key, value] of Object.entries(Service.flatten(payload))) {
            uri.searchParams.append(key, value);
        }

        return uri;
    }
{% endif %}
{% endfor %}
}
