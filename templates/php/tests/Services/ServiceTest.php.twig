{% import _self as helper %}
{% macro mockParameterValue(parameter) -%}
{%- if (parameter | enumValues) is not empty -%}
{%- if (parameter | schemaType) == 'array' -%}
[{{ (parameter | enumName) | caseUcfirst }}::{{ ((parameter | enumKeys)[0] ?? (parameter | enumValues)[0]) | caseEnumKey }}()]
{%- else -%}
{{ (parameter | enumName) | caseUcfirst }}::{{ ((parameter | enumKeys)[0] ?? (parameter | enumValues)[0]) | caseEnumKey }}()
{%- endif -%}
{%- elseif (parameter | schemaType) == 'object' -%}
[]
{%- elseif (parameter | schemaType) == 'array' -%}
[]
{%- elseif (parameter | schemaType) == 'file' -%}
InputFile::withData('', 'image/png')
{%- elseif (parameter | schemaType) == 'boolean' -%}
true
{%- elseif (parameter | schemaType) == 'string' -%}
"{% if (parameter | schemaExample) is not empty %}{{ (parameter | schemaExample) | escapeDollarSign }}{% endif %}"
{%- elseif (parameter | schemaType) == 'integer' -%}
1
{%- elseif (parameter | schemaType) == 'number' -%}
1.0
{%- else -%}
{{ (parameter | schemaExample) }}
{%- endif -%}
{%- endmacro %}
<?php

declare(strict_types=1);

namespace {{ namespace | caseNamespace }}\Services;

use {{ namespace | caseNamespace }}\Client;
use Mockery;
use Mockery\MockInterface;
use PHPUnit\Framework\TestCase;
{% set added = [] %}
{% for method in (service | operations) %}
{% for parameter in (method | parameters('all')) %}
{% if parameter.required and (parameter | schemaType) == 'file' and 'InputFile' not in added %}
use {{ namespace | caseNamespace }}\InputFile;
{% set added = added|merge(['InputFile']) %}
{% endif %}
{% if parameter.required and (parameter | enumName) is not empty and (parameter | enumName) not in added %}
use {{ namespace | caseNamespace }}\Enums\{{ (parameter | enumName) | caseUcfirst }};
{% set added = added|merge([(parameter | enumName)]) %}
{% endif %}
{% endfor %}
{% endfor %}

final class {{ service.name | caseUcfirst }}Test extends TestCase
{
    private Client&MockInterface $client;
    private {{ service.name | caseUcfirst }} ${{ service.name | caseCamel }};

    protected function setUp(): void
    {
        $this->client = Mockery::mock(Client::class);
        $this->{{ service.name | caseCamel }} = new {{ service.name | caseUcfirst }}($this->client);
    }

{% set nonDeprecatedMethodNames = [] %}
{% for method in (service | operations) %}
{% if not method.deprecated %}
{% set nonDeprecatedMethodNames = nonDeprecatedMethodNames|merge([(method | methodName) | caseCamel | lower]) %}
{% endif %}
{% endfor %}
{% for method in (service | operations) %}
{% set methodNameLower = (method | methodName) | caseCamel | lower %}
{% if method.deprecated and methodNameLower in nonDeprecatedMethodNames %}
{# Skip deprecated methods that have namespace collisions with non-deprecated methods #}
{% else %}
{% set responseModels = method | getResponseModels(spec) %}
{% set filteredResponseModelNames = method | getValidResponseModels %}
{% set responseDiscriminator = (method | responseDiscriminator) %}
{% set responseDefinitionName = '' %}
{% if filteredResponseModelNames is not empty %}
{% set responseDefinitionName = filteredResponseModelNames | last %}
{% elseif (method | responseModel) and (method | responseModel) != 'any' %}
{% set responseDefinitionName = (method | responseModel) %}
{% endif %}
{% set requiredParameters = (method | parameters('all')) | filter((param) => param.required) %}
    public function testMethod{{ (method | methodName) | caseUcfirst }}(): void
    {
{% if responseModels | length > 0 %}
{% if responseModels | length > 1 and responseDefinitionName and responseDiscriminator[responseDefinitionName] is defined %}
        $data = array_replace(
            {{ responseDefinitionName | mockDefinitionPayload(spec, 4) | raw -}},
            [
{% for field, expectedValue in responseDiscriminator[responseDefinitionName] %}
                "{{ field | escapeDollarSign }}" => "{{ expectedValue }}"{{ loop.last ? '' : ',' }}
{% endfor %}
            ]
        );
{% else %}
        $data = {{ responseDefinitionName | mockDefinitionPayload(spec, 3) | raw -}};
{% endif %}
{% elseif ((method | responseModel) and (method | responseModel) == 'any') or (method | getReturn(spec) == 'array') %}
        $data = [];
{% else %}
        $data = '';
{% endif %}

        $this->client
            ->allows()->call(Mockery::any(), Mockery::any(), Mockery::any(), Mockery::any(){% if (method | methodType) == 'webAuth' %}, Mockery::any(){% endif %})
            ->andReturn($data);
        $this->client
            ->allows()->getConfig(Mockery::any())
            ->andReturn('');

{% if requiredParameters | length > 0 %}
        $response = $this->{{ service.name | caseCamel }}->{{ (method | methodName) | caseCamel }}(
{% for parameter in requiredParameters %}
            {{ helper.mockParameterValue(parameter) }}{{ loop.last ? '' : ',' }}
{% endfor %}
        );
{% else %}
        $response = $this->{{ service.name | caseCamel }}->{{ (method | methodName) | caseCamel }}();
{% endif %}

{% if responseModels | length > 0 %}
        $this->assertInstanceOf({{ responseModels | last }}::class, $response);
{% else %}
        $this->assertSame($data, $response);
{% endif %}
    }
{% endif %}
{% endfor %}
}
