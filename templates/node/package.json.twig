{
  "name": "{{ language.params.npmPackage|caseDash }}",
  "homepage": "{{ spec.contactURL }}",
  "description": "{{ sdk.shortDescription }}",
  "version": "{{ sdk.version }}",
  "license": "{{ sdk.license }}",
  "main": "index.js",
  "repository": {
    "type": "git",
    "url": "{{ sdk.gitURL }}"
  },
  "devDependencies": {},
  "dependencies": {
    "axios": "^0.20.0",
    "form-data": "^3.0.0"
  }
}