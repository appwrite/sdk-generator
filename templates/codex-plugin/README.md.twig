# {{ spec.info.title }} Codex CLI Plugin

![License](https://img.shields.io/github/license/{{ sdk.gitUserName|url_encode }}/{{ sdk.gitRepoName|url_encode }}.svg?style=flat-square)
![Version](https://img.shields.io/badge/api%20version-{{ sdk.version }}-blue.svg?style=flat-square)
[![Twitter Account](https://img.shields.io/twitter/follow/appwrite?color=00acee&label=twitter&style=flat-square)](https://twitter.com/appwrite)
[![Discord](https://img.shields.io/discord/564160730845151244?label=discord&style=flat-square)](https://appwrite.io/discord)

{{ spec.info.title }} tools for the [OpenAI Codex CLI](https://github.com/openai/codex). This plugin packages {{ spec.info.title }} SDK skills, {{ spec.info.title }} deployment workflow skills, and the hosted {{ spec.info.title }} MCP server.

## Structure

```text
plugins/{{ spec.info.title | caseLower }}/skills/                       # Codex skills (auto-loaded by description match)
|-- {{ spec.info.title | caseLower }}-typescript/SKILL.md
|-- {{ spec.info.title | caseLower }}-dart/SKILL.md
|-- {{ spec.info.title | caseLower }}-kotlin/SKILL.md
|-- {{ spec.info.title | caseLower }}-swift/SKILL.md
|-- {{ spec.info.title | caseLower }}-php/SKILL.md
|-- {{ spec.info.title | caseLower }}-python/SKILL.md
|-- {{ spec.info.title | caseLower }}-ruby/SKILL.md
|-- {{ spec.info.title | caseLower }}-go/SKILL.md
|-- {{ spec.info.title | caseLower }}-rust/SKILL.md
|-- {{ spec.info.title | caseLower }}-dotnet/SKILL.md
|-- {{ spec.info.title | caseLower }}-cli/SKILL.md
|-- {{ spec.info.title | caseLower }}-deploy-site/SKILL.md
`-- {{ spec.info.title | caseLower }}-deploy-function/SKILL.md
plugins/{{ spec.info.title | caseLower }}/.codex-plugin/plugin.json     # Codex plugin manifest
.agents/plugins/marketplace.json         # Local marketplace manifest
plugins/{{ spec.info.title | caseLower }}/.mcp.json                     # MCP server config for plugin installs
plugins/{{ spec.info.title | caseLower }}/config.toml                   # MCP server config snippet for manual installs
```

## Installation

Codex plugins include a `.codex-plugin/plugin.json` manifest for plugin installation. For manual installs, Codex CLI loads skills from `~/.codex/skills/` and reads MCP server configuration from `~/.codex/config.toml`. Install the generated files manually by copying them into those locations.

To install through a local Codex marketplace, add this directory as a marketplace source using the generated `.agents/plugins/marketplace.json` file.

### macOS / Linux

```sh
# Copy skills
mkdir -p ~/.codex/skills
cp -R plugins/{{ spec.info.title | caseLower }}/skills/* ~/.codex/skills/

# Append the MCP config snippet to your Codex config
mkdir -p ~/.codex
cat plugins/{{ spec.info.title | caseLower }}/config.toml >> ~/.codex/config.toml
```

### Windows (PowerShell)

```powershell
# Copy skills
New-Item -ItemType Directory -Force -Path "$HOME\.codex\skills" | Out-Null
Copy-Item -Recurse plugins\{{ spec.info.title | caseLower }}\skills\* "$HOME\.codex\skills\"

# Append the MCP config snippet to your Codex config
New-Item -ItemType Directory -Force -Path "$HOME\.codex" | Out-Null
Get-Content plugins\{{ spec.info.title | caseLower }}\config.toml | Add-Content "$HOME\.codex\config.toml"
```

## Included Skills

Each skill ships as `plugins/{{ spec.info.title | caseLower }}/skills/{{ spec.info.title | caseLower }}-<language>/SKILL.md`. Codex loads them based on the `description` field in the frontmatter, so they activate automatically when a session is about Appwrite work in that language:

- `{{ spec.info.title | caseLower }}-typescript`
- `{{ spec.info.title | caseLower }}-dart`
- `{{ spec.info.title | caseLower }}-kotlin`
- `{{ spec.info.title | caseLower }}-swift`
- `{{ spec.info.title | caseLower }}-php`
- `{{ spec.info.title | caseLower }}-python`
- `{{ spec.info.title | caseLower }}-ruby`
- `{{ spec.info.title | caseLower }}-go`
- `{{ spec.info.title | caseLower }}-rust`
- `{{ spec.info.title | caseLower }}-dotnet`
- `{{ spec.info.title | caseLower }}-cli`
- `{{ spec.info.title | caseLower }}-deploy-site`
- `{{ spec.info.title | caseLower }}-deploy-function`

The language skills give Codex language-specific {{ spec.info.title }} context for authentication, database queries, storage, realtime, functions, and server-side administration. The deployment skills walk through {{ spec.info.title }} Sites and Functions deployment with the {{ spec.info.title }} CLI.

## Deployment Workflows

Deployment workflows are exposed as explicit skills:

- `${{ spec.info.title | caseLower }}-deploy-site`
- `${{ spec.info.title | caseLower }}-deploy-function`

Use these to walk through deploying {{ spec.info.title }} sites and functions with the {{ spec.info.title }} CLI. Codex can also load them automatically when a task matches the skill description.

## Hosted MCP Server

The bundled plugin and `plugins/{{ spec.info.title | caseLower }}/config.toml` register one `{{ spec.info.title | caseLower }}` server at `https://mcp.appwrite.io/`. It provides tools for working with your {{ spec.info.title }} workspace and projects and for searching the latest {{ spec.info.title }} documentation.

The hosted server uses browser-based OAuth, so no API key, project ID, endpoint, or other manually configured secret is required. After installing the plugin or merging the manual configuration, authenticate with:

```sh
codex mcp login {{ spec.info.title | caseLower }}
```

Then try:

> Use {{ spec.info.title }} to show my workspace context and list my projects.

If the browser does not open, use the authorization URL printed by Codex. Run `codex mcp logout {{ spec.info.title | caseLower }}` followed by `codex mcp login {{ spec.info.title | caseLower }}` to refresh the OAuth session, and use `codex mcp get {{ spec.info.title | caseLower }}` to inspect the server configuration and status.

## Verify

After copying, run `codex` and check:

- A session about {{ spec.info.title }} TypeScript work loads the `{{ spec.info.title | caseLower }}-typescript` skill (visible in the session header).
- Typing `$` lets you invoke the `{{ spec.info.title | caseLower }}-deploy-*` skills explicitly.
- `codex mcp list` (or `codex --debug`) lists the `{{ spec.info.title | caseLower }}` server.

## Contribution

This library is auto-generated by Appwrite custom [SDK Generator](https://github.com/appwrite/sdk-generator). To learn more about how you can help us improve this SDK, please check the [contribution guide](https://github.com/appwrite/sdk-generator/blob/master/CONTRIBUTING.md) before sending a pull-request.

## License

Please see the [{{spec.info.license.name}} license]({{spec.info.license.url}}) file for more information.
