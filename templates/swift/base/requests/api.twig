        return try await client.call(
            method: "{{ method.method | caseUpper }}",
            path: apiPath,
            headers: apiHeaders,
            params: apiParams{% if method.responseModel %},
            converter: converter
            {%~ endif %}
        )