using System;
using UnityEngine;

namespace {{ spec.info.title | caseUcfirst }}
{
    // Define the service enum with Flags attribute for multi-selection in the inspector
    [Flags]
    public enum {{ spec.info.title | caseUcfirst }}Service
    {
        None = 0,
{% for service in services %}
        {{ service.name | caseUcfirst }} = 1 << {{ loop.index0 }},
{% endfor %}

        [Tooltip("Selects all available services.")]
        All = {% for service in services %}{{ service.name | caseUcfirst }}{% if not loop.last %} | {% endif %}{% endfor %}

    }

    /// <summary>
    /// ScriptableObject configuration for {{ spec.info.title | caseUcfirst }} client settings
    /// </summary>
    [CreateAssetMenu(fileName = "{{ spec.info.title | caseUcfirst }}Config", menuName = "{{ spec.info.title | caseUcfirst }}/Configuration")]
    public class {{ spec.info.title | caseUcfirst }}Config : ScriptableObject
    {
        [Header("Connection Settings")]
        [Tooltip("Endpoint URL for {{ spec.info.title | caseUcfirst }} API (e.g., https://cloud.{{ spec.info.title | lower }}.io/v1)")]
        [SerializeField] private string endpoint = "https://cloud.{{ spec.info.title | lower }}.io/v1";

        [Tooltip("WebSocket endpoint for realtime updates (optional)")]
        [SerializeField] private string realtimeEndpoint = "wss://cloud.{{ spec.info.title | lower }}.io/v1";

        [Tooltip("Enable if using a self-signed SSL certificate")]
        [SerializeField] private bool selfSigned;

        [Header("Project Settings")]
        [Tooltip("Your {{ spec.info.title | caseUcfirst }} project ID")]
        [SerializeField] private string projectId = "";

        [Header("Service Initialization")]
        [Tooltip("Select which {{ spec.info.title | caseUcfirst }} services to initialize.")]
        [SerializeField] private {{ spec.info.title | caseUcfirst }}Service servicesToInitialize = {{ spec.info.title | caseUcfirst }}Service.All;

        [Header("Advanced Settings")]
        [Tooltip("Dev key (optional). Dev keys allow bypassing rate limits and CORS errors in your development environment. WARNING: Storing dev keys in ScriptableObjects is a security risk. Do not expose this in public repositories. Consider loading from a secure location at runtime for production builds.")]
        [SerializeField] private string devKey = "";

        [Tooltip("Automatically connect to {{ spec.info.title | caseUcfirst }} on start")]
        [SerializeField] private bool autoConnect;

        public string Endpoint => endpoint;
        public string RealtimeEndpoint => realtimeEndpoint;
        public bool SelfSigned => selfSigned;
        public string ProjectId => projectId;
        public string DevKey => devKey;
        public bool AutoConnect => autoConnect;
        public {{ spec.info.title | caseUcfirst }}Service ServicesToInitialize => servicesToInitialize;

        /// <summary>
        /// Validate configuration settings
        /// </summary>
        private void OnValidate()
        {
            if (string.IsNullOrEmpty(endpoint))
                Debug.LogWarning("{{ spec.info.title | caseUcfirst }}Config: Endpoint is required");

            if (string.IsNullOrEmpty(projectId))
                Debug.LogWarning("{{ spec.info.title | caseUcfirst }}Config: Project ID is required");

            if (!string.IsNullOrEmpty(devKey))
                Debug.LogWarning("{{ spec.info.title | caseUcfirst }}Config: Dev Key is set. For security, avoid storing keys directly in assets for production builds.");
        }


        /// <summary>
        /// Create a client using this configuration.
        /// </summary>
        public Client CreateClient()
        {
            return string.IsNullOrEmpty(devKey)
                ? Client.From(
                    projectId: projectId,
                    endpoint: endpoint,
                    endpointRealtime: string.IsNullOrEmpty(realtimeEndpoint) ? null : realtimeEndpoint,
                    selfSigned: selfSigned)
                : Client.FromDevKey(
                    projectId: projectId,
                    devKey: devKey,
                    endpoint: endpoint,
                    endpointRealtime: string.IsNullOrEmpty(realtimeEndpoint) ? null : realtimeEndpoint,
                    selfSigned: selfSigned);
        }

#if UNITY_EDITOR
        [UnityEditor.MenuItem("{{ spec.info.title | caseUcfirst }}/Create Configuration")]
        public static {{ spec.info.title | caseUcfirst }}Config CreateConfiguration()
        {
            var config = CreateInstance<{{ spec.info.title | caseUcfirst }}Config>();

            if (!System.IO.Directory.Exists("Assets/{{ spec.info.title | caseUcfirst }}"))
            {
                UnityEditor.AssetDatabase.CreateFolder("Assets", "{{ spec.info.title | caseUcfirst }}");
            }
            if (!System.IO.Directory.Exists("Assets/{{ spec.info.title | caseUcfirst }}/Resources"))
            {
                UnityEditor.AssetDatabase.CreateFolder("Assets/{{ spec.info.title | caseUcfirst }}", "Resources");
            }

            string path = "Assets/{{ spec.info.title | caseUcfirst }}/Resources/{{ spec.info.title | caseUcfirst }}Config.asset";
            path = UnityEditor.AssetDatabase.GenerateUniqueAssetPath(path);

            UnityEditor.AssetDatabase.CreateAsset(config, path);
            UnityEditor.AssetDatabase.SaveAssets();
            UnityEditor.EditorUtility.FocusProjectWindow();
            UnityEditor.Selection.activeObject = config;

            Debug.Log($"{{ spec.info.title | caseUcfirst }} configuration created at: {path}");

            return config;
        }
#endif
    }
}
