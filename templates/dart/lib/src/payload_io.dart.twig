import 'dart:io';

import '../payload.dart';

class PayloadIO extends Payload {
  // Write Payload to a file
  Future<File> toFile(String path) async {
    final file = File(path);
    return await file.writeAsBytes(data!);
  }
}
