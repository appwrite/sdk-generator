module Appwrite
    class Exception < StandardError
        def initialize(message, code = 0, response = nil)
            super(message)
            @code = code
            @response = response
        end
        attr_reader :code
        attr_reader :response
    end
end
