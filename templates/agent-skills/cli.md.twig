{% set lang = 'cli' %}
{% set langName = 'CLI' %}
{{ include('agent-skills/base/frontmatter.md.twig') }}

# {{ spec.title }} {{ langName }}

{{ spec.title }}'s command-line interface for managing {{ spec.title }} resources directly from the terminal. Supports user management, database operations, file storage, function deployment, and project configuration — ideal for scripting, CI/CD pipelines, and quick administrative tasks.

## When to Apply

Reference this skill when:

- Managing {{ spec.title }} resources from the terminal or shell scripts
- Creating, listing, or deleting users, databases, tables, or rows via CLI
- Uploading files or managing storage buckets from the command line
- Deploying or executing {{ spec.title }} Functions
- Automating {{ spec.title }} operations in CI/CD pipelines
- Quickly prototyping or debugging {{ spec.title }} API calls

## Setting Up the Client

```bash
{{ spec.title | caseLower }} login
{{ spec.title | caseLower }} client setEndpoint --endpoint="{{ spec.endpoint }}"
{{ spec.title | caseLower }} client setProject --projectId="[PROJECT_ID]"
```

## Code Examples

### User Management

```bash
# List users
{{ spec.title | caseLower }} users list

# Create user
{{ spec.title | caseLower }} users create \
    --userId="unique()" \
    --email="user@example.com" \
    --password="password123" \
    --name="User Name"

# Get user
{{ spec.title | caseLower }} users get --userId="[USER_ID]"

# Delete user
{{ spec.title | caseLower }} users delete --userId="[USER_ID]"
```

### Database Operations

> **Note:** Use `TablesDB` (not the deprecated `Databases` class) for all new code. Only use `Databases` if the existing codebase already relies on it or the user explicitly requests it.

```bash
# Create database
{{ spec.title | caseLower }} tablesDB create \
    --databaseId="unique()" \
    --name="My Database"

# Create row
{{ spec.title | caseLower }} tablesDB createRow \
    --databaseId="[DATABASE_ID]" \
    --tableId="[TABLE_ID]" \
    --rowId="unique()" \
    --data='{"title": "Hello World"}'

# List rows
{{ spec.title | caseLower }} tablesDB listRows \
    --databaseId="[DATABASE_ID]" \
    --tableId="[TABLE_ID]"

# Delete row
{{ spec.title | caseLower }} tablesDB deleteRow \
    --databaseId="[DATABASE_ID]" \
    --tableId="[TABLE_ID]" \
    --rowId="[ROW_ID]"
```

### File Storage

```bash
# Create bucket
{{ spec.title | caseLower }} storage createBucket \
    --bucketId="unique()" \
    --name="My Bucket"

# Upload file
{{ spec.title | caseLower }} storage createFile \
    --bucketId="[BUCKET_ID]" \
    --fileId="unique()" \
    --file="./path/to/file.png"

# List files
{{ spec.title | caseLower }} storage listFiles --bucketId="[BUCKET_ID]"

# Delete file
{{ spec.title | caseLower }} storage deleteFile \
    --bucketId="[BUCKET_ID]" \
    --fileId="[FILE_ID]"
```

### Serverless Functions

```bash
# Create function
{{ spec.title | caseLower }} functions create \
    --functionId="unique()" \
    --name="My Function" \
    --runtime="node-18.0"

# Deploy function
{{ spec.title | caseLower }} functions createDeployment \
    --functionId="[FUNCTION_ID]" \
    --entrypoint="index.js" \
    --code="."

# Execute function
{{ spec.title | caseLower }} functions createExecution \
    --functionId="[FUNCTION_ID]" \
    --body='{"key": "value"}'

# List executions
{{ spec.title | caseLower }} functions listExecutions --functionId="[FUNCTION_ID]"
```

