
using System;
using System.Linq;
using System.Collections.Generic;
using System.Text.Json;
using System.Text.Json.Serialization;
using {{ spec.info.title | caseUcfirst }}.Enums;
using {{ spec.info.title | caseUcfirst }}.Extensions;

namespace {{ spec.info.title | caseUcfirst }}.Models
{
    public class {{ definitionName | caseUcfirst | overrideIdentifier }}
    {
        {%~ for property in definition.properties %}
        [JsonPropertyName("{{ (property | schemaName) }}")]
        public {{ sub_schema(property, property | schemaRequired) | raw }} {{ property_name(definitionName, property | schemaName) | overrideProperty(definitionName) }} { get; private set; }

        {%~ endfor %}
        {%~ if definition.additionalProperties %}
        public Dictionary<string, object> {{ 'data' | caseUcfirst | escapeKeyword | removeDollarSign }} { get; private set; }

        {%~ endif %}
        public {{ definitionName | caseUcfirst | overrideIdentifier }}(
            {%~ for property in definition.properties %}
            {{ sub_schema(property, property | schemaRequired) | raw }} {{ (property | schemaName) | caseCamel | escapeKeyword }}{% if not loop.last or (loop.last and definition.additionalProperties) %},{% endif %}

            {%~ endfor %}
            {%~ if definition.additionalProperties %}
            Dictionary<string, object> {{ 'data' | caseCamel | escapeKeyword | removeDollarSign }}
            {%~ endif %}
        )
        {
            {%~ for property in definition.properties %}
            {{ property_name(definitionName, property | schemaName) | overrideProperty(definitionName) }} = {{ (property | schemaName) | caseCamel | escapeKeyword }};
            {%~ endfor %}
            {%~ if definition.additionalProperties %}
            {{ 'data' | caseUcfirst | escapeKeyword | removeDollarSign }} = {{ 'data' | caseCamel | escapeKeyword | removeDollarSign }};
            {%~ endif %}
        }

        public static {{ definitionName | caseUcfirst | overrideIdentifier }} From(Dictionary<string, object> map) => new {{ definitionName | caseUcfirst | overrideIdentifier }}(
            {%~ for property in definition.properties %}
            {{ (property | schemaName) | caseCamel | escapeKeyword | removeDollarSign }}:{{' '}}
                    {%- if (property | schemaModel) %}
                        {%- if (property | schemaType) == 'array' -%}
                            {%- if not (property | schemaRequired) -%}
                                map.TryGetValue("{{ (property | schemaName) }}", out var arrayRaw{{ loop.index }}) && arrayRaw{{ loop.index }} != null
                                    ? arrayRaw{{ loop.index }}.ConvertToList<Dictionary<string, object>>().Select(it => {{ spec.info.title | caseUcfirst }}.Models.{{ (property | schemaModel) | caseUcfirst | overrideIdentifier }}.From(map: it)).ToList()
                                    : null
                            {%- else -%}
                                map["{{ (property | schemaName) }}"].ConvertToList<Dictionary<string, object>>().Select(it => {{ spec.info.title | caseUcfirst }}.Models.{{ (property | schemaModel) | caseUcfirst | overrideIdentifier }}.From(map: it)).ToList()
                            {%- endif %}
                        {%- else -%}
                            {%- if not (property | schemaRequired) -%}
                                map.TryGetValue("{{ (property | schemaName) }}", out var objectRaw{{ loop.index }}) && objectRaw{{ loop.index }} != null
                                    ? {{ spec.info.title | caseUcfirst }}.Models.{{ (property | schemaModel) | caseUcfirst | overrideIdentifier }}.From(map: objectRaw{{ loop.index }} is JsonElement jsonObj{{ loop.index }} ? jsonObj{{ loop.index }}.Deserialize<Dictionary<string, object>>()! : (Dictionary<string, object>)objectRaw{{ loop.index }})
                                    : null
                            {%- else -%}
                                {{ spec.info.title | caseUcfirst }}.Models.{{ (property | schemaModel) | caseUcfirst | overrideIdentifier }}.From(map: map["{{ (property | schemaName) }}"] is JsonElement jsonObj{{ loop.index }} ? jsonObj{{ loop.index }}.Deserialize<Dictionary<string, object>>()! : (Dictionary<string, object>)map["{{ (property | schemaName) }}"])
                            {%- endif %}
                        {%- endif %}
                {%- elseif property.enum %}
                    {%- set enumName = (property | enumName) -%}
                    {%- if not (property | schemaRequired) -%}
                        map.TryGetValue("{{ (property | schemaName) }}", out var enumRaw{{ loop.index }})
                            ? enumRaw{{ loop.index }} == null
                                ? null
                                : new {{ spec.info.title | caseUcfirst }}.Enums.{{ enumName | caseUcfirst }}(enumRaw{{ loop.index }}.ToString()!)
                            : null
                    {%- else -%}
                        new {{ spec.info.title | caseUcfirst }}.Enums.{{ enumName | caseUcfirst }}(map["{{ (property | schemaName) }}"].ToString()!)
                    {%- endif %}
                {%- else %}
                    {%- if (property | schemaType) == 'array' -%}
                        {%- if not (property | schemaRequired) -%}
                            map.TryGetValue("{{ (property | schemaName) }}", out var arrayRaw{{ loop.index }}) && arrayRaw{{ loop.index }} != null
                                ? arrayRaw{{ loop.index }}.ConvertToList<{{ property | typeName | slice(5, (property | typeName | length) - 6) | raw }}>()
                                : null
                        {%- else -%}
                            map["{{ (property | schemaName) }}"].ConvertToList<{{ property | typeName | slice(5, (property | typeName | length) - 6) | raw }}>()
                        {%- endif %}
                    {%- else %}
                        {%- if (property | schemaType) == "integer" or (property | schemaType) == "number" %}
                            {%- if not (property | schemaRequired) -%}
                                map.TryGetValue("{{ (property | schemaName) }}", out var numberRaw{{ loop.index }}) && numberRaw{{ loop.index }} != null
                                    ? Convert.To{% if (property | schemaType) == "integer" %}Int64{% else %}Double{% endif %}(numberRaw{{ loop.index }})
                                    : null
                            {%- else -%}
                                Convert.To{% if (property | schemaType) == "integer" %}Int64{% else %}Double{% endif %}(map["{{ (property | schemaName) }}"])
                            {%- endif %}
                        {%- else %}
                            {%- if (property | schemaType) == "boolean" -%}
                                {%- if not (property | schemaRequired) -%}
                                    map.TryGetValue("{{ (property | schemaName) }}", out var boolRaw{{ loop.index }}) && boolRaw{{ loop.index }} != null
                                        ? ({{ property | typeName }}?)boolRaw{{ loop.index }}
                                        : null
                                {%- else -%}
                                    ({{ property | typeName }})map["{{ (property | schemaName) }}"]
                                {%- endif %}
                            {%- else %}
                                {%- if (property | schemaType) == "object" -%}
                                    {%- if not (property | schemaRequired) -%}
                                        map.TryGetValue("{{ (property | schemaName) }}", out var objectRaw{{ loop.index }}) ? objectRaw{{ loop.index }} : null
                                    {%- else -%}
                                        map["{{ (property | schemaName) }}"]
                                    {%- endif %}
                                {%- else %}
                                    {%- if not (property | schemaRequired) -%}
                                        map.TryGetValue("{{ (property | schemaName) }}", out var {{ (property | schemaName) | caseCamel | escapeKeyword | removeDollarSign }}) ? {{ (property | schemaName) | caseCamel | escapeKeyword | removeDollarSign }}?.ToString() : null
                                    {%- else -%}
                                        map["{{ (property | schemaName) }}"].ToString()
                                    {%- endif %}
                                {%- endif %}
                            {%- endif %}
                        {%- endif %}
                    {%- endif %}
                {%~ endif %}
                {%- if not loop.last or (loop.last and definition.additionalProperties) %},
                {%~ endif %}
            {%~ endfor %}
            {%- if definition.additionalProperties %}
            {{ 'data' | caseCamel | escapeKeyword | removeDollarSign }}: map.TryGetValue("{{ 'data' }}", out var additionalPropsValue)
                ? (Dictionary<string, object>)additionalPropsValue
                : map
            {%- endif ~%}
        );

        public Dictionary<string, object?> ToMap() => new Dictionary<string, object?>()
        {
            {%~ for property in definition.properties %}
            { "{{ (property | schemaName) }}", {{ property | toMapValue(property_name(definitionName, property | schemaName) | overrideProperty(definitionName)) | raw }} }{% if not loop.last or (loop.last and definition.additionalProperties) %},{% endif %}

            {%~ endfor %}
            {%~ if definition.additionalProperties %}
            { "{{ 'data' }}", {{ 'data' | caseUcfirst | escapeKeyword | removeDollarSign }} }
            {%~ endif %}
        };
        {%~ if definition.additionalProperties %}

        public T ConvertTo<T>(Func<Dictionary<string, object>, T> fromJson) =>
            fromJson.Invoke({{ 'data' | caseUcfirst | escapeKeyword | removeDollarSign }});
        {%~ endif %}
        {%~ for property in definition.properties %}
        {%~ if (property | schemaModel) %}
        {%~ for def in definitions %}
        {%~ if def.name == (property | schemaModel) and def.additionalProperties and (property | schemaType) == 'array' %}

        public T ConvertTo<T>(Func<Dictionary<string, object>, T> fromJson) =>
            (T){{ (property | schemaName) | caseUcfirst | escapeKeyword }}.Select(it => it.ConvertTo(fromJson));

        {%~ endif %}
        {%~ endfor %}
        {%~ endif %}
        {%~ endfor %}
    }
}
