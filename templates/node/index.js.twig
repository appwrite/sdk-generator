const Client = require('./lib/client.js');
{% for service in spec.services %}
const {{service.name | caseUcfirst}} = require('./lib/services/{{service.name | caseDash}}.js');
{% endfor %}

module.export = {
    Client: Client,
{% for service in spec.services %}
    {{service.name | caseUcfirst}}: {{service.name | caseUcfirst}},
{% endfor %}
};