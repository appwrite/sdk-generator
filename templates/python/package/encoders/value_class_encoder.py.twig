import json
from ..models.base_model import AppwriteModel
{%~ for enum in allEnums %}
from ..enums.{{ enum.title | caseSnake }} import {{ enum.title | caseUcfirst | overrideIdentifier }}
{%~ endfor %}


class ValueClassEncoder(json.JSONEncoder):
    def default(self, o):
        if isinstance(o, AppwriteModel):
            return o.to_dict()

        {%~ for enum in allEnums %}
        if isinstance(o, {{ enum.title | caseUcfirst | overrideIdentifier }}):
            return o.value

        {%~ endfor %}
        return super().default(o)
