class Role {
    static any = () => {
        return 'any'
    }
    static user = (id, status = '') => {
        if(status === '') {
            return `user:${id}`
        }
        return `user:${id}/${status}`
    }
    static users = (status = '') => {
        if(status === '') {
            return 'users'
        }
        return `users/${status}`
    }
    static guests = () => {
        return 'guests'
    }
    static team = (id, role = '') => {
        if(role === '') {
            return 'team:' + id
        }
        return 'team:' + id + '/' + role
    }
    static member = (id) => {
        return 'member:' + id
    }
}

module.exports = Role;