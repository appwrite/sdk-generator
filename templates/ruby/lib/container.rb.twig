require 'net/http'
require 'uri'
require 'json'
require 'mime/types'

require_relative '{{ spec.title | caseSnake }}/client'
require_relative '{{ spec.title | caseSnake }}/service'
require_relative '{{ spec.title | caseSnake }}/exception'
require_relative '{{ spec.title | caseSnake }}/payload'
require_relative '{{ spec.title | caseSnake }}/query'
require_relative '{{ spec.title | caseSnake }}/permission'
require_relative '{{ spec.title | caseSnake }}/role'
require_relative '{{ spec.title | caseSnake }}/id'

{% for defintion in spec.definitions %}
require_relative '{{ spec.title | caseSnake }}/models/{{ defintion.name | caseSnake }}'
{% endfor %}

{% for enum in spec.enums %}
require_relative '{{ spec.title | caseSnake }}/enums/{{ enum.name | caseSnake }}'
{% endfor %}

{% for service in spec.services %}
require_relative '{{ spec.title | caseSnake }}/services/{{ service.name | caseSnake }}'
{% endfor %}