{% macro sub_schema(property) %}{% if (property | schemaModel) %}{% if (property | schemaType) == 'array' %}List<{{(property | schemaModel) | caseUcfirst}}>{% else %}{{(property | schemaModel) | caseUcfirst}}{% endif %}{% else %}{{property | typeName}}{% endif %}{% endmacro %}
#frozen_string_literal: true

module {{ spec.info.title | caseUcfirst }}
    module Models
        class {{ definitionName | caseUcfirst }}
{% for property in definition.properties %}
            attr_reader :{{ (property | schemaName) | caseSnake | escapeKeyword }}
{% endfor %}
{% if definition.additionalProperties %}
            attr_reader :{{ 'data' | caseSnake | escapeKeyword | removeDollarSign }}
{% endif %}

            def initialize(
{% for property in definition.properties %}
                {{ (property | schemaName) | caseSnake | escapeKeyword }}:{% if not (property | schemaRequired) %} {{ property.default }}{% endif %}{% if not loop.last or (loop.last and definition.additionalProperties) %},{% endif %}

{% endfor %}
{% if definition.additionalProperties %}
                {{ 'data' | caseSnake | escapeKeyword | removeDollarSign }}:
{% endif %}
            )
{% for property in definition.properties %}
{% if property.enum %}
{% if (property | schemaRequired) %}
                @{{ (property | schemaName) | caseSnake | escapeKeyword }} = validate_{{ (property | schemaName) | caseSnake | escapeKeyword }}({{ (property | schemaName) | caseSnake | escapeKeyword }})
{% else %}
                @{{ (property | schemaName) | caseSnake | escapeKeyword }} = {{ (property | schemaName) | caseSnake | escapeKeyword }}.nil? ? {{ (property | schemaName) | caseSnake | escapeKeyword }} : validate_{{ (property | schemaName) | caseSnake | escapeKeyword }}({{ (property | schemaName) | caseSnake | escapeKeyword }})
{% endif %}
{% else %}
                @{{ (property | schemaName) | caseSnake | escapeKeyword }} = {{ (property | schemaName) | caseSnake | escapeKeyword }}
{% endif %}
{% endfor %}
{% if definition.additionalProperties %}
                @{{ 'data' | caseSnake | escapeKeyword | removeDollarSign }} = {{ 'data' | caseSnake | escapeKeyword | removeDollarSign }}
{% endif %}
            end

            def self.from(map:)
                {{ definitionName | caseUcfirst }}.new(
{% for property in definition.properties %}
                    {{ (property | schemaName) | caseSnake | escapeKeyword | removeDollarSign }}: {% if (property | schemaModel) %}{% if (property | schemaType) == 'array' %}map["{{ (property | schemaName) }}"]{% if not (property | schemaRequired) %}&.{% else %}.{% endif %}map { |it| {{ (property | schemaModel) | caseUcfirst }}.from(map: it) }{% else %}{% if not (property | schemaRequired) %}map["{{(property | schemaName) }}"].nil? ? nil : {% endif %}{{(property | schemaModel) | caseUcfirst}}.from(map: map["{{(property | schemaName) }}"]){% endif %}{% else %}map["{{ (property | schemaName) }}"]{% endif %}{% if not loop.last or (loop.last and definition.additionalProperties) %},{% endif %}

{% endfor %}
{% if definition.additionalProperties %}
                    {{ 'data' | caseSnake | escapeKeyword | removeDollarSign }}: map["{{ 'data' }}"] || map
{% endif %}
                )
            end

            def to_map
                {
{% for property in definition.properties %}
                    "{{ (property | schemaName) }}": {% if (property | schemaModel) %}{% if (property | schemaType) == 'array' %}@{{ (property | schemaName) | caseSnake | escapeKeyword | removeDollarSign }}{% if not (property | schemaRequired) %}&.{% else %}.{% endif %}map { |it| it.to_map }{% else %}@{{(property | schemaName) | caseSnake | escapeKeyword | removeDollarSign }}{% if not (property | schemaRequired) %}&.{% else %}.{% endif %}to_map{% endif %}{% else %}@{{(property | schemaName) | caseSnake | escapeKeyword }}{% endif %}{% if not loop.last or (loop.last and definition.additionalProperties) %},{% endif %}

{% endfor %}
{% if definition.additionalProperties %}
                    "{{ 'data' }}": @{{ 'data' | caseSnake | escapeKeyword | removeDollarSign }}
{% endif %}
                }
            end
{% if definition.additionalProperties %}

            def convert_to(from_json)
                from_json.call({{ 'data' | caseSnake | escapeKeyword | removeDollarSign }})
            end
{% endif %}
{% for property in definition.properties %}
{% if (property | schemaModel) %}
{% for def in definitions %}
{% if def.name == (property | schemaModel) and def.additionalProperties and (property | schemaType) == 'array' %}

            def convert_to(from_json)
                {{ (property | schemaName) | caseSnake | escapeKeyword }}.map { |it| it.convert_to(from_json) }
            end
{% endif %}
{% endfor %}
{% endif %}
{% endfor %}
{% if definition.properties | filter(p => p.enum) | length > 0 %}

            private

{% endif %}
{% for property in definition.properties %}
{% if property.enum %}
            def validate_{{ (property | schemaName) | caseSnake | escapeKeyword }}({{ (property | schemaName) | caseSnake | escapeKeyword }})
                valid_{{ (property | schemaName) | caseSnake }} = [
{% for value in property.enum %}
                    {{ spec.info.title | caseUcfirst }}::Enums::{{ (property | enumName) | caseUcfirst }}::{{ ((property | enumKeys)[loop.index0] ?? value) | caseEnumKey }},
{% endfor %}
                ]

                unless valid_{{ (property | schemaName) | caseSnake }}.include?({{ (property | schemaName) | caseSnake | escapeKeyword }})
                    raise ArgumentError, "Invalid " + {{ (property | schemaName) | caseSnake | escapeKeyword }} + ". Must be one of: " + valid_{{ (property | schemaName) | caseSnake }}.join(', ')
                end

                {{ (property | schemaName) | caseSnake | escapeKeyword }}
            end

{% endif %}
{% endfor %}
        end
    end
end
