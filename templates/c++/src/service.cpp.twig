#include "client.hpp"
#include "service.hpp"

namespace {{ spec.title | caseUcfirst }} {

/**
 * @param Client client
 */
explicit Service::Service(Client client) {
    this->client = &client;
}
} // namespace {{ spec.title | caseUcfirst }}
