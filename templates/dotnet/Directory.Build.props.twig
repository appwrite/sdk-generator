<Project>
  <PropertyGroup>
    <NuGetAudit Condition="'$(MSBuildProjectName)' == '{{ spec.info.title | caseUcfirst }}'">true</NuGetAudit>
    <NuGetAuditMode Condition="'$(MSBuildProjectName)' == '{{ spec.info.title | caseUcfirst }}'">all</NuGetAuditMode>

    <!-- Fail only on high/critical -->
    <NuGetAuditLevel Condition="'$(MSBuildProjectName)' == '{{ spec.info.title | caseUcfirst }}'">high</NuGetAuditLevel>

    <WarningsAsErrors Condition="'$(MSBuildProjectName)' == '{{ spec.info.title | caseUcfirst }}'">$(WarningsAsErrors);NU1903;NU1904</WarningsAsErrors>
  </PropertyGroup>
</Project>
