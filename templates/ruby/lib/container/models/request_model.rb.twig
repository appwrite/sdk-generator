# frozen_string_literal: true

module {{ spec.info.title | caseUcfirst }}
    module Models
        class {{ requestModelName | caseUcfirst }}
{% for property in requestModel.properties %}
            attr_reader :{{ (property | schemaName) | caseSnake | escapeKeyword }}
{% endfor %}

            def initialize(
{% for property in requestModel.properties %}
                {{ (property | schemaName) | caseSnake | escapeKeyword }}:{% if not (property | schemaRequired) %} nil{% endif %}{% if not loop.last %},{% endif %}

{% endfor %}
            )
{% for property in requestModel.properties %}
{% if property | usesEnumType %}
{% if (property | schemaRequired) %}
                @{{ (property | schemaName) | caseSnake | escapeKeyword }} = validate_{{ (property | schemaName) | caseSnake | escapeKeyword }}({{ (property | schemaName) | caseSnake | escapeKeyword }})
{% else %}
                @{{ (property | schemaName) | caseSnake | escapeKeyword }} = {{ (property | schemaName) | caseSnake | escapeKeyword }}.nil? ? {{ (property | schemaName) | caseSnake | escapeKeyword }} : validate_{{ (property | schemaName) | caseSnake | escapeKeyword }}({{ (property | schemaName) | caseSnake | escapeKeyword }})
{% endif %}
{% else %}
                @{{ (property | schemaName) | caseSnake | escapeKeyword }} = {{ (property | schemaName) | caseSnake | escapeKeyword }}
{% endif %}
{% endfor %}
            end

            def to_map
                {
{% for property in requestModel.properties %}
                    "{{ (property | schemaName) }}": {% if (property | schemaModel) %}{% if (property | schemaType) == 'array' %}@{{ (property | schemaName) | caseSnake | escapeKeyword | removeDollarSign }}&.map { |it| it.to_map }{% else %}@{{(property | schemaName) | caseSnake | escapeKeyword | removeDollarSign }}&.to_map{% endif %}{% else %}@{{(property | schemaName) | caseSnake | escapeKeyword }}{% endif %}{% if not loop.last %},{% endif %}

{% endfor %}
                }
            end

            def to_json(*args)
                to_map.to_json(*args)
            end
{% if requestModel.properties | filter(p => p | usesEnumType) | length > 0 %}

            private

{% endif %}
{% for property in requestModel.properties %}
{% if property | usesEnumType %}
            def validate_{{ (property | schemaName) | caseSnake | escapeKeyword }}({{ (property | schemaName) | caseSnake | escapeKeyword }})
                valid_{{ (property | schemaName) | caseSnake }} = [
{% for value in property | enumValues %}
                    {{ spec.info.title | caseUcfirst }}::Enums::{{ (property | enumName) | caseUcfirst }}::{{ ((property | enumKeys)[loop.index0] ?? value) | caseEnumKey }},
{% endfor %}
                ]

                unless valid_{{ (property | schemaName) | caseSnake }}.include?({{ (property | schemaName) | caseSnake | escapeKeyword }})
                    raise ArgumentError, "Invalid " + {{ (property | schemaName) | caseSnake | escapeKeyword }}.to_s + ". Must be one of: " + valid_{{ (property | schemaName) | caseSnake }}.join(', ')
                end

                {{ (property | schemaName) | caseSnake | escapeKeyword }}
            end

{% endif %}
{% endfor %}
        end
    end
end
