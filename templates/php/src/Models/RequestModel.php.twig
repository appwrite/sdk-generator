<?php

declare(strict_types=1);

namespace {{ namespace | caseNamespace }}\Models;

{% set enumImports = [] %}
{% for property in requestModel.properties %}
{% if (property | enumValues) is not empty %}
{% set enumName = (property | enumName) %}
{% if enumName not in enumImports %}
{% set enumImports = enumImports|merge([enumName]) %}
{% endif %}
{% endif %}
{% endfor %}
{% for enumName in enumImports %}
use {{ namespace | caseNamespace }}\Enums\{{ enumName | caseUcfirst | overrideIdentifier }};
{% endfor %}
{% if enumImports is not empty %}
{% endif %}
{% set requiredProperties = requestModel.properties | filter((property) => (property | schemaRequired)) %}
{% set optionalProperties = requestModel.properties | filter((property) => not (property | schemaRequired)) %}
/**
 * {{ requestModel.description | unescape | raw }}
 *
 * @phpstan-consistent-constructor
 */
readonly class {{ requestModelName | caseUcfirst | overrideIdentifier }}
{
    use ArraySerializable;

    /**
     * {{ requestModelName | caseUcfirst }} constructor.
     *
{% for property in requiredProperties %}
{% set paramDocType = property | typeName %}
{% if (property | schemaType) == 'array' and (property | schemaModel) %}
{% set paramDocType = 'list<' ~ ((property | schemaModel) | caseUcfirst | overrideIdentifier) ~ '>' %}
{% elseif (property | schemaType) == 'array' and (property | enumValues) is not empty %}
{% set paramDocType = 'list<' ~ (((property | enumName)) | caseUcfirst | overrideIdentifier) ~ '>' %}
{% endif %}
     * @param {{ paramDocType | raw }} ${{ (property | schemaName) | caseCamel }} {{ property.description | trim | unescape | lower | raw }}
{% endfor %}
{% for property in optionalProperties %}
{% set paramDocType = property | typeName %}
{% if (property | schemaType) == 'array' and (property | schemaModel) %}
{% set paramDocType = 'list<' ~ ((property | schemaModel) | caseUcfirst | overrideIdentifier) ~ '>' %}
{% elseif (property | schemaType) == 'array' and (property | enumValues) is not empty %}
{% set paramDocType = 'list<' ~ (((property | enumName)) | caseUcfirst | overrideIdentifier) ~ '>' %}
{% endif %}
     * @param {{ paramDocType | raw }}|null ${{ (property | schemaName) | caseCamel }} {{ property.description | trim | unescape | lower | raw }}
{% endfor %}
     */
    public function __construct(
{% for property in requiredProperties %}
        public {{ property | typeName }} ${{ (property | schemaName) | caseCamel }}{{ (not loop.last or optionalProperties is not empty) ? ',' : '' }}
{% endfor %}
{% for property in optionalProperties %}
        public ?{{ property | typeName }} ${{ (property | schemaName) | caseCamel }} = null{{ loop.last ? '' : ',' }}
{% endfor %}
    ) {
    }

    /**
     * @param array<string, mixed> $data
     */
    public static function from(array $data): static
    {
{% if requiredProperties is empty and optionalProperties is empty %}
        return new static();
{% else %}
{% for property in requiredProperties %}
        if (!array_key_exists('{{ (property | schemaName) }}', $data)) {
            throw new \InvalidArgumentException('Missing required field "{{ (property | schemaName) }}" for ' . static::class . '.');
        }
{% endfor %}

        return new static(
{% for property in requiredProperties %}
            {{ (property | schemaName) | caseCamel }}: {% if (property | schemaType) == 'array' and (property | schemaModel) %}is_array($data['{{ (property | schemaName) }}'])
                ? array_map(
                    static fn (mixed $item): mixed => static::hydrateTypedValue({{ (property | schemaModel) | caseUcfirst | overrideIdentifier }}::class, $item),
                    $data['{{ (property | schemaName) }}']
                )
                : $data['{{ (property | schemaName) }}']{% elseif (property | schemaModel) %}static::hydrateTypedValue({{ (property | schemaModel) | caseUcfirst | overrideIdentifier }}::class, $data['{{ (property | schemaName) }}']){% elseif (property | schemaType) == 'array' and (property | enumValues) is not empty %}is_array($data['{{ (property | schemaName) }}'])
                ? array_map(
                    static fn (mixed $item): mixed => static::hydrateTypedValue({{ ((property | enumName)) | caseUcfirst | overrideIdentifier }}::class, $item),
                    $data['{{ (property | schemaName) }}']
                )
                : [static::hydrateTypedValue({{ ((property | enumName)) | caseUcfirst | overrideIdentifier }}::class, $data['{{ (property | schemaName) }}'])]{% elseif (property | enumValues) is not empty %}static::hydrateTypedValue({{ ((property | enumName)) | caseUcfirst | overrideIdentifier }}::class, $data['{{ (property | schemaName) }}']){% else %}$data['{{ (property | schemaName) }}']{% endif %}{{ (not loop.last or optionalProperties is not empty) ? ',' : '' }}
{% endfor %}
{% for property in optionalProperties %}
            {{ (property | schemaName) | caseCamel }}: array_key_exists('{{ (property | schemaName) }}', $data)
{% if (property | schemaType) == 'array' and (property | schemaModel) %}
                ? (
                    is_array($data['{{ (property | schemaName) }}'])
                        ? array_map(
                            static fn (mixed $item): mixed => static::hydrateTypedValue({{ (property | schemaModel) | caseUcfirst | overrideIdentifier }}::class, $item),
                            $data['{{ (property | schemaName) }}']
                        )
                        : $data['{{ (property | schemaName) }}']
                )
{% elseif (property | schemaModel) %}
                ? static::hydrateTypedValue({{ (property | schemaModel) | caseUcfirst | overrideIdentifier }}::class, $data['{{ (property | schemaName) }}'], true)
{% elseif (property | schemaType) == 'array' and (property | enumValues) is not empty %}
                ? (
                    is_array($data['{{ (property | schemaName) }}'])
                        ? array_map(
                            static fn (mixed $item): mixed => static::hydrateTypedValue({{ ((property | enumName)) | caseUcfirst | overrideIdentifier }}::class, $item),
                            $data['{{ (property | schemaName) }}']
                        )
                        : [static::hydrateTypedValue({{ ((property | enumName)) | caseUcfirst | overrideIdentifier }}::class, $data['{{ (property | schemaName) }}'])]
                )
{% elseif (property | enumValues) is not empty %}
                ? static::hydrateTypedValue({{ ((property | enumName)) | caseUcfirst | overrideIdentifier }}::class, $data['{{ (property | schemaName) }}'], true)
{% else %}
                ? $data['{{ (property | schemaName) }}']
{% endif %}
                : null{{ loop.last ? '' : ',' }}
{% endfor %}
        );
{% endif %}
    }

    /**
     * @return array<string, mixed>
     */
    public function toArray(): array
    {
{% if requestModel.properties is empty %}
        return [];
{% else %}
        return [
{% for property in requestModel.properties %}
            '{{ (property | schemaName) }}' => static::serializeValue($this->{{ (property | schemaName) | caseCamel }}){{ loop.last ? '' : ',' }}
{% endfor %}
        ];
{% endif %}
    }
}
