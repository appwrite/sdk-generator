{% macro sub_schema(property) %}{% if (property | schemaModel) %}{% if (property | schemaType) == 'array' %}List<{{(property | schemaModel) | caseUcfirst | overrideIdentifier}}>{% else %}{{(property | schemaModel) | caseUcfirst | overrideIdentifier }}{% endif %}{% else %}{% if (property | schemaType) == 'object' and property.additionalProperties %}Map<String, dynamic>{% else %}{{property | typeName}}{% endif %}{% endif %}{% endmacro %}
part of '../../models.dart';

/// {{ definition.description }}
class {{ definitionName | caseUcfirst | overrideIdentifier }} implements Model {
{% for property in definition.properties %}
    /// {{ property.description }}
    final {% if not (property | schemaRequired) %}{{_self.sub_schema(property)}}? {{ (property | schemaName) | escapeKeyword }}{% else %}{{_self.sub_schema(property)}} {{ (property | schemaName) | escapeKeyword }}{% endif %};

{% endfor %}
{%~ if definition.additionalProperties %}
    final Map<String, dynamic> {{ 'data' | escapeKeyword }};

{% endif %}
    {{ definitionName | caseUcfirst | overrideIdentifier}}({% if definition.properties | length or definition.additionalProperties %}{{ '{' }}{% endif %}

{% for property in definition.properties %}
        {% if (property | schemaRequired) %}required {% endif %}this.{{ (property | schemaName) | escapeKeyword }},
{% endfor %}
{% if definition.additionalProperties %}
        required this.{{ 'data' | escapeKeyword }},
{% endif %}
    {% if definition.properties | length or definition.additionalProperties %}{{ '}' }}{% endif %});

    factory {{ definitionName | caseUcfirst | overrideIdentifier}}.fromMap(Map<String, dynamic> map) {
        return {{ definitionName | caseUcfirst | overrideIdentifier }}(
{% for property in definition.properties %}
            {{ (property | schemaName) | escapeKeyword }}:{{' '}}
            {%- if (property | schemaModel) -%}
                {%- if (property | schemaType) == 'array' -%}
                    {%- if not (property | schemaRequired) -%}map['{{(property | schemaName) | escapeDollarSign }}'] != null ? {% endif -%}
                    List<{{(property | schemaModel) | caseUcfirst | overrideIdentifier}}>.from(map['{{(property | schemaName) | escapeDollarSign }}'].map((p) => {{(property | schemaModel) | caseUcfirst | overrideIdentifier}}.fromMap(p)))
                    {%- if not (property | schemaRequired) %} : null{% endif -%}
                {%- else -%}
                    {%- if not (property | schemaRequired) -%}map['{{(property | schemaName) | escapeDollarSign }}'] != null ? {% endif -%}
                    {{(property | schemaModel) | caseUcfirst | overrideIdentifier}}.fromMap(map['{{(property | schemaName) | escapeDollarSign }}'])
                    {%- if not (property | schemaRequired) %} : null{% endif -%}
                {%- endif -%}
            {%- elseif property.enum -%}
                {%- set enumName = (property | enumName) -%}
                {%- if (property | schemaRequired) -%}
                enums.{{ enumName | caseUcfirst }}.values.firstWhere((e) => e.value == map['{{(property | schemaName) | escapeDollarSign }}'])
                {%- else -%}
                map['{{(property | schemaName) | escapeDollarSign }}'] != null ? enums.{{ enumName | caseUcfirst }}.values.firstWhere((e) => e.value == map['{{(property | schemaName) | escapeDollarSign }}']) : null
                {%- endif -%}
            {%- else -%}
                {%- if (property | schemaType) == 'array' -%}
                    List.from(map['{{(property | schemaName) | escapeDollarSign }}'] ?? [])
                {%- else -%}
                    map['{{(property | schemaName) | escapeDollarSign }}']
                    {%- if (property | schemaType) == "number" -%}
                        {%- if not (property | schemaRequired) %}?{% endif %}.toDouble()
                    {%- endif -%}
                    {%- if (property | schemaType) == "string" -%}
                        {%- if not (property | schemaRequired) %}?{% endif %}.toString()
                    {%- endif -%}
                {%- endif -%}
            {%- endif -%},
{% endfor %}
{% if definition.additionalProperties %}
            {{ 'data' | escapeKeyword }}: map["{{ 'data' | escapeDollarSign }}"] ?? map,
{% endif %}
        );
    }

    @override
    Map<String, dynamic> toMap() {
        return {
{% for property in definition.properties %}
            "{{ (property | schemaName) | escapeDollarSign }}": {{ property | modelToMapValue(property | schemaName, property | schemaRequired) }},
{% endfor %}
{% if definition.additionalProperties %}
            "{{ 'data' | escapeDollarSign }}": {{ 'data' | escapeKeyword }},
{% endif %}
        };
    }
{% if definition.additionalProperties %}

    T convertTo<T>(T Function(Map<String, dynamic>) fromJson) => fromJson({{ 'data' | escapeKeyword }});
{% endif %}
{% for property in definition.properties %}
{% if (property | schemaModel) %}
{% for def in definitions %}
{% if def.name == (property | schemaModel) and def.additionalProperties and (property | schemaType) == 'array' %}

    List<T> convertTo<T>(T Function(Map) fromJson) =>
        {{(property | schemaName)}}.map((d) => d.convertTo<T>(fromJson)).toList();
{% endif %}
{% endfor %}
{% endif %}
{% endfor %}
}
