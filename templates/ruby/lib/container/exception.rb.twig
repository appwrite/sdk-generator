module Appwrite
    class Exception < StandardError
        @code = 0;
        @response = nil;

        def initialize(message, code, response)
            super(message)
            @code = code
            @response = response
        end
        attr_reader :code
        attr_reader :response
    end
end