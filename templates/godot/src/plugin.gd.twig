@tool
extends EditorPlugin

func _enter_tree():
	add_autoload_singleton("{{sdk.name}}", "res://addons/{{sdk.name | caseLower}}/{{sdk.name | caseLower}}.gd")

func _exit_tree():
	remove_autoload_singleton("{{sdk.name}}")
