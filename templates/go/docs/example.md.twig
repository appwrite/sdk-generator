package main

import (
    "fmt"
    "github.com/{{ sdk.gitUserName|url_encode }}/{{ sdk.gitRepoName|url_encode }}"
)

func main() {
    var client := {{ spec.title | caseLower }}.Client{}

{% for node in method.security %}
{% for key,header in node|keys %}
    client.Set{{header}}("")
{% endfor %}
{% endfor %}

    var service := {{ spec.title | caseLower }}.{{ service.name | caseUcfirst }}{
        client: &client
    }

    var response, error := service.{{ method.name | caseUcfirst }}({% for parameter in method.parameters.all %}{% if parameter.required %}{% if not loop.first %}, {% endif %}{{ parameter | paramExample }}{% endif %}{% endfor %})

    if error != nil {
        panic(error)
    }

    fmt.Println(response)
}