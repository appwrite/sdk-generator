use crate::client::{Client, ParamType};
use std::collections::HashMap;

#[derive(Clone)]
pub struct {{ service.name | caseUcfirst }} {
  client: Client
}

impl {{ service.name | caseUcfirst }} {  
  pub fn new(client: &Client) -> Self {
    Self {
      client: client.clone()
    }
  }
{% for method in service.methods %}

{% if method.description %}
{{ method.description|dartComment }}
{% endif %}
    pub fn {{ method.name | caseSnake }}(&self{% if method.parameters.all|length >= 1 %}, {% endif %}{% for parameter in method.parameters.all %}{{ parameter.name | caseSnake | escapeKeyword}}: {{ parameter.type | typeName }}{% if not loop.last %}, {% endif %}{% endfor %}) -> reqwest::blocking::Response {
        let path = "{{ method.path|replace({'{': '', '}': ""}) }}"{% for parameter in method.parameters.path %}.replace("{{ parameter.name | caseSnake }}", &{{ parameter.name | caseSnake }}){% endfor %};

{% if method.headers %}
    let headers: HashMap<String, String> = [
{% for parameter in method.parameters.header %}
      ("{{ parameter.name }}".to_string(), "{{ parameter.name | caseCamel }}".to_string()),
{% endfor %}
{% for key, header in method.headers %}
      ("{{ key }}".to_string(), "{{ header }}".to_string()),
{% endfor %}
      ].iter().cloned().collect();
{% endif %}

    let params: HashMap<String, ParamType> = [
{% for parameter in method.parameters.query %}
      ("{{ parameter.name }}".to_string(), {% if parameter.type == 'integer' %} ParamType::Number({% elseif parameter.type == 'string' %}ParamType::String({% elseif parameter.type == 'array' %}ParamType::Array({% elseif parameter.type == 'boolean' %}ParamType::Bool({% elseif parameter.type == 'object' %}ParamType::Object({% elseif parameter.type == 'file' %}ParamType::FilePath({% endif %}{{ parameter.name | caseSnake | escapeKeyword }}{% if parameter.type == 'array' %}.into_iter().map(|x| ParamType::String(x)).collect(){% endif %}{% if parameter.type == 'string' %}.to_string(){% endif %}{% if parameter.type == 'object' %}.unwrap(){% endif %})),
{% endfor %}
{% for parameter in method.parameters.body %}
      ("{{ parameter.name }}".to_string(), {% if parameter.type == 'integer' %} ParamType::Number({% elseif parameter.type == 'string' %}ParamType::String({% elseif parameter.type == 'array' %}ParamType::Array({% elseif parameter.type == 'boolean' %}ParamType::Bool({% elseif parameter.type == 'object' %}ParamType::Object({% elseif parameter.type == 'file' %}ParamType::FilePath({% endif %}{{ parameter.name | caseSnake | escapeKeyword }}{% if parameter.type == 'array' %}.into_iter().map(|x| ParamType::String(x)).collect(){% endif %}{% if parameter.type == 'string' %}.to_string(){% endif %}{% if parameter.type == 'object' %}.unwrap(){% endif %})),
{% endfor %}
      ].iter().cloned().collect();

      return self.client.clone().call("{{ method.method | caseUpper }}", &path, {% if method.headers %}Some(headers){% else %}None{% endif %}, Some(params) );
    }
{% endfor %}
}