<?php

namespace {{ spec.namespace | caseNamespace }}\Models;

{% set enumImports = [] %}
{% for property in requestModel.properties %}
{% if property.enumValues is not empty or property.enumName is not empty %}
{% set enumName = property.enumName ?? property.name %}
{% if enumName not in enumImports %}
{% set enumImports = enumImports|merge([enumName]) %}
{% endif %}
{% endif %}
{% endfor %}
{% for enumName in enumImports %}
use {{ spec.namespace | caseNamespace }}\Enums\{{ enumName | caseUcfirst | overrideIdentifier }};
{% endfor %}
{% if enumImports is not empty %}
{% endif %}
{% set requiredProperties = requestModel.properties | filter((property) => property.required) %}
{% set optionalProperties = requestModel.properties | filter((property) => not property.required) %}
/**
 * {{ requestModel.description | unescape | raw }}
 */
readonly class {{ requestModel.name | caseUcfirst | overrideIdentifier }}
{
    use ArraySerializable;

{% set fieldMappings = [] %}
{% for property in requestModel.properties %}
{% set propertyName = property.name | caseCamel | escapeKeyword %}
{% if propertyName != property.name %}
{% set fieldMappings = fieldMappings|merge([{ property: propertyName, field: property.name }]) %}
{% endif %}
{% endfor %}
{% if fieldMappings is not empty %}
    /**
     * @var array<string, string>
     */
    private const FIELD_MAP = [
{% for mapping in fieldMappings %}
        '{{ mapping.property }}' => '{{ mapping.field }}'{{ loop.last ? '' : ',' }}
{% endfor %}
    ];

{% endif %}
    /**
     * {{ requestModel.name | caseUcfirst }} constructor.
     *
{% for property in requiredProperties %}
{% set paramDocType = property | typeName %}
{% if property.type == 'array' and property.sub_schema %}
{% set paramDocType = 'list<' ~ (property.sub_schema | caseUcfirst | overrideIdentifier) ~ '>' %}
{% elseif property.type == 'array' and (property.enumValues is not empty or property.enumName is not empty) %}
{% set paramDocType = 'list<' ~ ((property.enumName ?? property.name) | caseUcfirst | overrideIdentifier) ~ '>' %}
{% endif %}
     * @param {{ paramDocType | raw }} ${{ property.name | caseCamel | escapeKeyword }} {{ property.description | unescape | lower | raw }}
{% endfor %}
{% for property in optionalProperties %}
{% set paramDocType = property | typeName %}
{% if property.type == 'array' and property.sub_schema %}
{% set paramDocType = 'list<' ~ (property.sub_schema | caseUcfirst | overrideIdentifier) ~ '>' %}
{% elseif property.type == 'array' and (property.enumValues is not empty or property.enumName is not empty) %}
{% set paramDocType = 'list<' ~ ((property.enumName ?? property.name) | caseUcfirst | overrideIdentifier) ~ '>' %}
{% endif %}
     * @param {{ paramDocType | raw }}|null ${{ property.name | caseCamel | escapeKeyword }} {{ property.description | unescape | lower | raw }}
{% endfor %}
     */
    public function __construct(
{% for property in requiredProperties %}
        public {{ property | typeName }} ${{ property.name | caseCamel | escapeKeyword }}{{ (not loop.last or optionalProperties is not empty) ? ',' : '' }}
{% endfor %}
{% for property in optionalProperties %}
        public ?{{ property | typeName }} ${{ property.name | caseCamel | escapeKeyword }} = null{{ loop.last ? '' : ',' }}
{% endfor %}
    ) {
    }

    /**
     * @param array<string, mixed> $data
     */
    public static function from(array $data): static
    {
{% if requiredProperties is empty and optionalProperties is empty %}
        return new static();
{% else %}
{% for property in requiredProperties %}
        if (!array_key_exists('{{ property.name }}', $data)) {
            throw new \InvalidArgumentException('Missing required field "{{ property.name }}" for ' . static::class . '.');
        }
{% endfor %}

        return new static(
{% for property in requiredProperties %}
            {{ property.name | caseCamel | escapeKeyword }}: {% if property.type == 'array' and property.sub_schema %}is_array($data['{{ property.name }}'])
                ? array_map(
                    static fn (mixed $item): mixed => static::hydrateTypedValue({{ property.sub_schema | caseUcfirst | overrideIdentifier }}::class, $item),
                    $data['{{ property.name }}']
                )
                : $data['{{ property.name }}']{% elseif property.sub_schema %}static::hydrateTypedValue({{ property.sub_schema | caseUcfirst | overrideIdentifier }}::class, $data['{{ property.name }}']){% elseif property.type == 'array' and (property.enumValues is not empty or property.enumName is not empty) %}is_array($data['{{ property.name }}'])
                ? array_map(
                    static fn (mixed $item): mixed => static::hydrateTypedValue({{ (property.enumName ?? property.name) | caseUcfirst | overrideIdentifier }}::class, $item),
                    $data['{{ property.name }}']
                )
                : $data['{{ property.name }}']{% elseif property.enumValues is not empty or property.enumName is not empty %}static::hydrateTypedValue({{ (property.enumName ?? property.name) | caseUcfirst | overrideIdentifier }}::class, $data['{{ property.name }}']){% else %}$data['{{ property.name }}']{% endif %}{{ (not loop.last or optionalProperties is not empty) ? ',' : '' }}
{% endfor %}
{% for property in optionalProperties %}
            {{ property.name | caseCamel | escapeKeyword }}: array_key_exists('{{ property.name }}', $data)
                ? {% if property.type == 'array' and property.sub_schema %}is_array($data['{{ property.name }}'])
                    ? array_map(
                        static fn (mixed $item): mixed => static::hydrateTypedValue({{ property.sub_schema | caseUcfirst | overrideIdentifier }}::class, $item),
                        $data['{{ property.name }}']
                    )
                    : $data['{{ property.name }}']{% elseif property.sub_schema %}static::hydrateTypedValue({{ property.sub_schema | caseUcfirst | overrideIdentifier }}::class, $data['{{ property.name }}'], true){% elseif property.type == 'array' and (property.enumValues is not empty or property.enumName is not empty) %}is_array($data['{{ property.name }}'])
                    ? array_map(
                        static fn (mixed $item): mixed => static::hydrateTypedValue({{ (property.enumName ?? property.name) | caseUcfirst | overrideIdentifier }}::class, $item),
                        $data['{{ property.name }}']
                    )
                    : $data['{{ property.name }}']{% elseif property.enumValues is not empty or property.enumName is not empty %}static::hydrateTypedValue({{ (property.enumName ?? property.name) | caseUcfirst | overrideIdentifier }}::class, $data['{{ property.name }}'], true){% else %}$data['{{ property.name }}']{% endif %}
                : null{{ loop.last ? '' : ',' }}
{% endfor %}
        );
{% endif %}
    }

    /**
     * @return array<string, mixed>
     */
    public function toArray(): array
    {
{% if requestModel.properties is empty %}
        return [];
{% else %}
        return [
{% for property in requestModel.properties %}
            '{{ property.name }}' => static::serializeValue($this->{{ property.name | caseCamel | escapeKeyword }}){{ loop.last ? '' : ',' }}
{% endfor %}
        ];
{% endif %}
    }
}
