const { defineConfig } = require("tsup");

module.exports = defineConfig({
  entry: ["src/**/*.ts"],
  sourcemap: true,
  clean: true,
  dts: true,
  bundle: false,
  treeshake: true,
  outDir: "dist",
  outExtension({ format }) {
    return {
      cjs: '.js',
    }[format]
  },
  target: "node16",
  format: ['cjs']
})