enum {{ enum.name | caseUcfirst | overrideIdentifier }} : String {
{% for value in enum.enum %}
  {% set key = enum.keys is empty ? value : enum.keys[loop.index0] %}
  case {{ key | caseCamel | replace({'-': ''})}} = '{{ value }}'
{% endfor %}
}
