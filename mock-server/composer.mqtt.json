{
    "name": "utopia/mock-server-mqtt",
    "description": "Dependencies for app/mqtt.php (the mock MQTT broker), installed to ./vendor-mqtt. Kept separate from the HTTP mock's composer.json because the current utopia-php/mqtt (dev) needs utopia-php/telemetry ^0.4, which no utopia-php/framework release allows.",
    "repositories": [
        {
            "type": "vcs",
            "url": "https://github.com/utopia-php/mqtt"
        }
    ],
    "minimum-stability": "dev",
    "prefer-stable": true,
    "require": {
        "utopia-php/mqtt": "dev-dev"
    },
    "config": {
        "vendor-dir": "vendor-mqtt",
        "allow-plugins": {
            "php-http/discovery": true,
            "tbachert/spi": true
        }
    }
}
