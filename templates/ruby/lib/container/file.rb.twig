require 'mime/types'

module {{spec.title | caseUcfirst}}
    class AppwriteFile
        def initialize(path)
            @name = File.basename(path)
            @content = File.read(path)
            @mimeType = MIME::Types.type_for(value)
        end
    end
end