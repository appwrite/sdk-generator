{
  "name": "{{ language.params.npmPackage }}",
  "homepage": "{{ spec.contactURL }}",
  "description": "{{ sdk.shortDescription }}",
  "version": "{{ sdk.version }}",
  "license": "{{ sdk.license }}",
  "main": "dist/umd/sdk.js",
  "exports": {
    ".": {
      "import": "./dist/esm/sdk.js",
      "require": "./dist/umd/sdk.js"
    },
    "./package.json": "./package.json"
  },
  "module": "dist/esm/sdk.js",
  "types": "types/sdk.d.ts",
  "repository": {
    "type": "git",
    "url": "{{ sdk.gitURL }}"
  },
  "scripts": {
    "build": "npm run build:types && npm run build:esm && npm run build:umd",
    "build:types": "tsc --declaration --emitDeclarationOnly --outDir types",
    "build:esm": "tsc --module es6 --outDir dist/esm",
    "build:umd": "rollup dist/esm/sdk.js --format umd --name Appwrite --context window -m --no-esModule -o dist/umd/sdk.js"
  },
  "devDependencies": {
    "playwright": "^1.10.0",
    "rollup": "^2.43.0",
    "serve-handler": "^6.1.3",
    "typescript": "^4.2.3"
  },
  "dependencies": {},
  "jsdelivr": "dist/umd/sdk.js",
  "unpkg": "dist/umd/sdk.js"
}
