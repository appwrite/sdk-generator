/// {{spec.info.title | caseUcfirst}} Models
library {{ language.params.packageName }}.models;

{% if (requestEnums | length) > 0 or (responseEnums | length) > 0 %}
import 'enums.dart' as enums;
{% endif %}

part 'src/models/model.dart';
{% for definitionName, definition in definitions %}
part 'src/models/{{definitionName | caseSnake}}.dart';
{% endfor %}
{% for requestModelName, requestModel in requestModels %}
part 'src/models/{{requestModelName | caseSnake}}.dart';
{% endfor %}