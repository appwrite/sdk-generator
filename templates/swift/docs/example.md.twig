```swift
import Appwrite

func main() {
    let client = Client()
{% if method.security|length > 0 %}
      .setEndpoint("https://[HOSTNAME_OR_IP]/v1") // Your API Endpoint
{% for node in method.security %}
{% for key,header in node|keys %}
      .set{{header | caseUcfirst}}("{{node[header]['x-appwrite']['demo']}}") // {{node[header].description}}
{% endfor %}
{% endfor %}
{% endif %}

    let {{ service.name | caseCamel }} = {{ service.name | caseUcfirst }}(client: client)
    {{ service.name | caseCamel }}.{{ method.name | caseCamel }}({% if method.parameters.all | length == 0 %}) { {% if method.type != "webAuth" %}result in{% endif %}{% endif %}

{% for parameter in method.parameters.all | filter((param) => param.required) %}
        {{parameter.name}}: {{ parameter | paramExample | escapeKeyword }}{% if not loop.last %},{% endif %}

{% if loop.last %}
    ) { {% if method.type != "webAuth" %}result in{% endif %}

{% endif %}
{% endfor %}
        switch result {
        case .failure(let error):
            print(error)
        case .success(let response):
{% if method.type == 'webAuth' %}
{% elseif method.type == 'location' %}
            print(result) // Resource URL
{% else %}
            let json = response.body.string()
{% endif %}
        }
    }
}
```