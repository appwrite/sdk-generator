{
{%~ for property in definition.properties %}
    "{{ property.name | replace({'$': '_'}) }}": {{ property | paramExample }}{% if not loop.last or definition.additionalProperties %},{% endif %}

{%~ endfor %}
{%~ if definition.additionalProperties %}
{%~ for key, value in definition.additionalPropertiesExamples %}
    "{{ key }}": {{ value | paramExample }}{% if not loop.last %},{% endif %}

{%~ endfor %}
{%~ endif %}
}
